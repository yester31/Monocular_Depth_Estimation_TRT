# 앞으로의 작업 계획 — 추론 속도 기준

2026-08-13 작성. 리팩토링 완료 시점(`REFACTOR_PLAN.md` §10) 이후의 계획.
**우선순위 기준은 추론 속도다.** 그 기준으로 작업 순서를 정했다.

| 문서 | 역할 |
| --- | --- |
| `REFACTOR_PLAN.md` | **끝난 리팩토링의 기록.** 구조 정리·버그 수정·측정 통일 |
| **이 문서** | **앞으로 할 일.** 무엇을, 왜, 어떻게, 어떻게 검증할지 |
| `docs/model_contracts.md` | 모델 12종의 사실 관계와 측정 결과 |

---

## 1. 이 저장소로 하려는 것

1. **각 모델을 최대한 빠르게 만든다** ← **최우선**
2. 모델들을 공정하게 비교한다
3. 결과를 눈으로 확인한다 (포인트 클라우드 포함)
4. 지금 쓰는 최적화 방식이 최선인지 검토한다

리팩토링으로 2번은 거의 됐고, **1번은 거의 안 됐다.** 12개 엔진 전부
"TensorRT 기본 설정 + 16비트"이며 모델별로 최적을 찾은 적이 없다.

---

## 2. 지금 어디에 시간이 쓰이고 있나

| 모델 | 속도 | 입력 | 픽셀 수 | 인코더 | 정밀도 |
| --- | ---: | --- | ---: | --- | --- |
| depth_anything_v2 | **4.3 ms** | 518×518 | 268k | ViT-S | 16 |
| distill_any_depth | 10.0 | 518×518 | 268k | Small | 16 |
| depth_anything_ac | 10.1 | 518×518 | 268k | ViT-S | 16 |
| unidepth_v2 | 11.6 | 518×518 | 268k | **ViT-B** | 16 |
| unik3d | 14.5 | 518×518 | 268k | **ViT-B** | 16 |
| depth_anything_v3 | 19.4 | 518×518 | 268k | **LARGE** | 16 |
| moge_2 | 30.1 | 388×518 | 201k | ViT-S | 16 |
| vggt | 53.1 | 518×518 | 268k | 고정(1B) | 16 |
| streamvggt | 53.2 | 518×518 | 268k | 고정 | 16 |
| metric_anything | 69.3 | 388×518 | 201k | 고정 | 16 |
| **depth_pro** | **240.5** | **1536×1536** | **2,359k** | 고정 | 16 |
| **metric3d_v2** | **349.8** | 616×1064 | 655k | **ViT-L** | **32** |

**시간의 대부분이 아래 두 줄에 있다.** 그리고 그 두 줄이 유독 큰 이유는
모델이 특별히 무거워서가 아니라 **우리가 고른 설정 때문일 수 있다** —
하나는 픽셀이 8.8배 많고, 하나는 유일하게 제일 큰 인코더에 유일한 32비트다.

---

## 3. 속도 레버 — 큰 것부터

| | 레버 | 기대 효과 | 근거 |
| --- | --- | --- | --- |
| **1** | **입력 해상도** | **수 배** | 픽셀 수에 초선형. 실측: 518×518 14.4ms → 700×518 24.5ms — **픽셀 1.35배에 시간 1.70배** |
| **2** | **인코더 크기** | **3~5배** | ViT-L → ViT-S |
| **3** | **정밀도** | 32→16비트 **약 2배**, 8비트 추가 1.5~2배 | metric3d_v2만 32비트 |
| **4** | 그래프 정리 | 최대 **20%** | unidepth_v2 실측 (64비트 실수 제거로 14.35 → 11.47 ms) |
| **5** | uint8 입력 | **고정 0.24 ms/MB** | 3개 모델 실측, 편차 1% 이내 |
| **6** | 빌더 최적화 레벨 | 보통 0~10% | 미측정 |
| **7** | CUDA Graph | **1.5% 이하** | 구간 계측으로 상한 확인 |

**가장 중요한 결론: TensorRT 설정 튜닝(6·7)이 가장 작은 레버다.**
모델 자체의 선택(1·2·3)이 훨씬 크다. 흔히 "TensorRT 최적화"라고 하면 6·7을
떠올리지만, 이 저장소에서 실제로 시간을 좌우하는 것은 해상도와 모델 크기다.

**5번의 성질**: 절감은 비율이 아니라 **입력 바이트가 정하는 고정된 시간**이다.
518×518 이면 어느 모델이든 0.77 ms 를 아낀다. depth_anything_v2 에서 20.7%,
vggt 에서 1.95% 로 보이는 것은 절감이 달라서가 아니라 **기준 시간이 4.3ms 와
53ms 이기 때문**이다.

---

## 4. 작업 — 레버 순서대로

### S1. `metric3d_v2` 를 정상 급으로 — **단일 최대 개선**

**현재**: ViT-L + 32비트 + 616×1064 = **349.8 ms**. 12개 중 가장 느리다.

**문제는 두 개가 겹쳐 있다는 것이다.**

- 유일한 **ViT-L** (다른 모델은 ViT-S 또는 ViT-B)
- 유일한 **32비트** (다른 11개는 16비트)

```python
precision = "fp32"   # 'fp32'
```

주석이 선택지를 32비트 하나로 적어놨는데 **왜 그런지 아무 데도 없다.**
16비트를 시도했다 실패한 것인지, 안 해본 것인지 구분할 수 없다.

**어떻게**

1. 16비트로 빌드해본다 → 되면 약 2배
2. 작은 인코더로 export 해본다 → 되면 추가 3배 안팎
3. 안 되면 **왜 안 되는지 기록한다.** 지금은 그 기록조차 없다

**기대**: 349.8 → **60~80 ms 대**. 4~6배.

**확인 필요**: 작은 인코더 가중치가 실제로 공개돼 있는지. 코드에
`vits`/`vitl`/`vitg` 선택지가 있지만 다운로드 가능 여부는 별개다.

**대가**: 품질이 떨어진다. **얼마나 떨어지는지 지금은 잴 수 없다** — §5 참조.

### S2. 입력 해상도를 낮출 수 있는가

**`depth_pro` 가 다른 모델의 8.8배 픽셀을 처리한다.**

| 모델 | 픽셀 | 다른 모델 대비 |
| --- | ---: | ---: |
| depth_pro | 2,359k | **8.8배** |
| metric3d_v2 | 655k | 2.4배 |
| 나머지 | 201~268k | 1배 |

픽셀 수에 대해 시간이 **초선형**으로 늘기 때문에(어텐션이 토큰 수의 제곱),
해상도를 낮추면 그 이상으로 줄어든다. 저장소 실측이 이를 보여준다 —
픽셀 1.35배에 시간 1.70배.

**기대**: depth_pro 를 518×518 로 내릴 수 있다면 240 → **30~40 ms 대**.

**위험**: depth_pro 는 업스트림이 1536 전용으로 설계한 모델이다. 낮추면
동작 자체가 무너질 수 있다. **시도해봐야 안다.** 무너지면 그 사실을 기록하고
1536 을 유지한다 — 그것도 "이 모델은 이 비용이 필수"라는 결론이므로 가치가 있다.

**나머지 모델도 확인한다**: 지금 518×518 은 "비교를 위한 공통 크기"로 고른
값이지 각 모델의 최적이 아니다. 더 낮춰도 되는 모델이 있을 수 있다.

### S3. 인코더 크기 — 선택 가능한 모델을 작은 급으로

**전부 맞추는 것은 불가능하다.** 12개 중 4개는 선택지가 없다.

| 선택 가능 (8개) | 고정 (4개) |
| --- | --- |
| depth_anything_v2/ac/v3, distill_any_depth, moge_2, unidepth_v2, unik3d, metric3d_v2 | **depth_pro, metric_anything, vggt, streamvggt** |

VGGT 는 10억 파라미터 단일 모델이고 depth_pro 도 아키텍처가 고정이다.

**작은 급으로 내릴 여지가 있는 모델**

| 모델 | 현재 | 현재 속도 |
| --- | --- | ---: |
| `metric3d_v2` | **ViT-L** | 349.8 ms (S1 에서 처리) |
| `depth_anything_v3` | **LARGE** | 19.4 ms |
| `unidepth_v2` | ViT-B | 11.6 ms |
| `unik3d` | ViT-B | 14.5 ms |

나머지 4개는 이미 가장 작은 급이다.

### S4. 그래프 정리

**(1) 64비트 실수 잔존 검사 — 12개 전수**

`unidepth_v2` 에서 이것 하나로 **20% 가 나왔다** (14.35 → 11.47 ms).
그래프에 64비트가 섞여 있으면 GPU 가 조용히 32비트로 낮춰 쓰는데,
그 과정에서 **연산 융합이 막힌다.**

**나머지 11개는 검사한 적이 없다.** 도구는 이미 있다
(`core/onnx_tools.count_float64`). 반나절이면 전수 확인된다.

**(2) 그래프 단순화 기준 통일**

불필요한 연산 노드를 정리하는 단계가 모델마다 제각각이다.

| 켜짐 8개 | 꺼짐 3개 | 설정 자체가 없음 1개 |
| --- | --- | --- |
| | `depth_anything_ac`, `streamvggt`, `vggt` | `depth_pro` |

**왜 껐는지 아무 데도 적혀 있지 않다.** 켜서 export 해보고, 되면 켜고
안 되면 이유를 주석으로 남긴다.

### S5. uint8 입력 — 518×518·388×518 모델 전부

측정은 끝났다. 절감은 **입력 바이트가 정하는 고정된 시간**이다.

| 입력 크기 | 절감 | 판단 |
| --- | ---: | --- |
| 518×518 | **0.77 ms** | **적용** — 연산이 늘지 않음을 확인함 |
| 388×518 | 약 0.58 ms | **적용** (동일 성질로 추정, 확인 필요) |
| 1536×1536 | 6.77 ms 절감 − 2.99 ms 연산 증가 = **실질 3.8 ms** | 재확인 후 판단 |
| 616×1064 | 미측정 | 확인 필요 |

1536 에서만 연산이 늘었다. 700만 픽셀에 Cast·Div·Transpose 를 얹으면 그
자체가 일이고 TensorRT 가 첫 레이어에 접지 못했다. **작은 입력에서는 흡수된다.**

### S6. 빌더 최적화 레벨 0~5 스윕

TensorRT 가 엔진을 만들 때 "얼마나 열심히 찾을지"를 정하는 값. 12개 전부
기본값(3)이고 **다른 값을 시도한 적이 없다.**

- 12개 × 6단계 = **72번 빌드.** 사람 손은 안 가고 시간만 든다
- **결과가 안 바뀐다** → 정확도 재검증 불필요, 위험 없음
- `get_engine` 이 이미 이 값을 인자로 받고 엔진 지문에도 포함한다 —
  **스윕 스크립트만 있으면 된다**
- 타이밍 캐시를 재사용하면 빌드 시간이 크게 준다

**반드시 S1~S5 뒤에 한다.** 엔진을 바꾸는 결정이 남아 있으면 스윕 결과가
통째로 버려진다.

### S7. 보류 — 근거가 약하거나 비싼 것

| | 왜 보류인가 |
| --- | --- |
| **CUDA Graph** | **상한이 이미 측정됐다.** depth_anything_v2 에서 전송·연산·회수를 뺀 나머지(호출 오버헤드+동기화)는 **0.065 ms, 전체의 1.5%** 다. 그것이 CUDA Graph 가 노리는 부분이다. 단, GPU 안에서 커널 사이 빈 시간은 "연산"에 포함되어 이 숫자에 안 잡히므로 여지가 아주 없지는 않다 |
| **BF16** | RTX 3080 에서 16비트 대비 이득이 있는지 **불확실하다.** 소비자용 칩은 데이터센터 칩과 텐서코어 구성이 다르다. **착수 전에 이것부터 확인해야 한다** |
| **8비트(INT8)** | 1.5~2배 기대되지만 **교정용 데이터셋**이 따로 필요하고 정확도 기준을 다시 세워야 한다. 지금 기준(0.03~0.92%)은 확실히 깨진다. **품질 측정 수단이 갖춰진 뒤에** |

---

## 5. 속도를 밀기 전에 반드시 있어야 하는 것

**S1·S2·S3 는 전부 품질을 깎는다.** 그런데 지금은 얼마나 깎였는지 잴 수 없다.

### Q1. 품질 측정 수단 — 정답 데이터 (**가장 중요한 전제**)

**지금 "정확도 검증"이 무엇을 보는가**

```
TensorRT 엔진 결과  vs  그 엔진을 만든 ONNX 파일 결과
```

**변환 손실만** 본다. ViT-L 을 ViT-S 로 바꾸면 그건 **다른 모델**이라
이 검증으로는 아무것도 안 나온다 — 둘 다 "자기 파일과 일치"하기 때문이다.

**정답 없이 속도만 밀면 "빠른데 얼마나 나빠졌는지 모르는 엔진" 12개가 남는다.**
품질 하한선을 정하고 그 안에서 최대한 빠르게 하는 것이 최적화이고,
하한선을 못 재면 그냥 축소다.

**지표는 이미 있다.** `core/golden.py` 에 AbsRel·RMSE·δ1 이 구현돼 있고
비교할 정답이 없어서 못 쓰고 있을 뿐이다.

**깊이 정답만으로는 부족하다** — 12개가 서로 다른 것을 내놓는다.

| 모델이 내는 것 | 필요한 정답 | 해당 모델 |
| --- | --- | --- |
| 상대 깊이 | 깊이 (스케일·시프트 맞춘 뒤) | `depth_anything_v2/ac/v3`, `distill_any_depth` |
| 미터 깊이 | 깊이 (맞추지 않고) | `depth_pro` |
| 점 지도 | 깊이 **+ 카메라 내부 파라미터** | `unidepth_v2`, `unik3d`, `moge_2`, `metric_anything` |
| 초점거리·내부 파라미터 | **카메라 캘리브레이션** | `depth_pro`, `unidepth_v2` |
| canonical 깊이 | 깊이 + **실제 초점거리** | `metric3d_v2` |

**절반이 카메라를 추정하므로, 카메라 정보가 함께 있는 데이터가 훨씬 가치 있다.**

**후보** — 아래는 **기억에 근거한 것이며 검증되지 않았다.** 규격·라이선스·
배포 형태는 내려받기 전에 확인해야 한다.

| 데이터 | 성격 | 맞는 점 | 주의 |
| --- | --- | --- | --- |
| **NYU Depth V2** (테스트 654장) | 실내, 미터, 내부 파라미터 고정 | **논문 값과 대조 가능** — 배선이 틀리면 바로 드러난다 | 경계·반사면 부정확 |
| **KITTI** (Eigen 테스트 697장) | 실외 주행, LiDAR, 캘리브레이션 제공 | 실외 커버 | 정답이 희소 → 마스크 필수 |
| iBims-1 | 실내 소규모, 고품질 | 경계·평면성 | 장면 수 적음 |
| DIODE | 실내+실외 | 한 데이터로 양쪽 | 용량 큼 |
| Virtual KITTI 2 / Replica (합성) | 완벽한 깊이+포즈+내부 파라미터 | 점 지도·카메라까지 평가 | 도메인 차이 |

**피할 것: Hypersim.** `depth_anything_v2` 기본 체크포인트가 Hypersim 으로
학습돼 있어 그 모델에만 유리해진다.

**권장**: **NYU 계열 실내 1종부터.** 실외는 정답이 희소해 마스크 처리가
필요하므로, 실내에서 배선을 검증한 뒤에 한다.

**정렬 방식을 모델마다 선언한다** (`spec.json`). 상대 깊이 모델을 정렬 없이
평가하면 무조건 지고, 미터 모델을 정렬해서 평가하면 **미터를 맞히는 능력이
점수에서 사라진다.** 어느 쪽이냐가 결과를 뒤집으므로 코드에 흩어놓으면 안 된다.

**규모**: 데이터 확보 반나절, 평가 배선 1~2일, 12개 실행 반나절.

### Q2. 모델당 결과 여러 개 다루기 (도구)

**같은 모델의 결과가 여러 개일 때 도구들이 마지막 것만 남기고 덮어쓴다.**

```python
{r["model"]: r for r in load_bench(...)}   # models.py, package_artifacts.py 등 4곳
```

`compare.py` 는 이미 `variant` 열까지 갖추고 제대로 다룬다. 나머지 4곳이 안 된다.

**앞으로 할 작업 대부분이 모델당 여러 결과를 만든다.**

| 작업 | 모델당 결과 수 |
| --- | ---: |
| 16비트 vs 32비트 | 2 |
| 인코더 크기 비교 | 2~3 |
| **최적화 레벨 스윕** | **6** |
| uint8 적용 전후 | 2 |

**지금 상태로는 스윕 72개 결과를 저장조차 못 한다.** 결정 사항이 아니라
S 작업 대부분의 선행 조건이다. **반나절.**

### Q3. 전처리 일반화 (도구)

`core/preprocess.py` 는 완성되어 있다 — 리사이즈 6종, 정규화 5종, 모델별
차이를 데이터로 선언하는 구조까지. **그런데 12개 스크립트 중 아무도 쓰지 않고
테스트만 쓴다.** 각 스크립트는 여전히 자기 전처리를 직접 구현한다.

**속도 작업에 왜 필요한가**: S2(해상도 낮추기)를 하려면 여러 해상도로 전처리를
바꿔가며 돌려야 하는데, 지금은 스크립트마다 크기와 파일 이름이 하드코딩돼 있다.
**Q1(정답 데이터)도 데이터셋 이미지를 12개 모델에 넣어야 하므로 여기에 걸린다.**

**검증 방법이 이미 저장소에 있다**: `reports/inputs/*.npy` 12개는 **엔진에
실제로 들어간 바로 그 텐서**다. 재작성이 맞는지 GPU 없이 **바이트 단위로**
확인할 수 있다. 같지 않으면 틀린 것이다 — 판단이 개입할 여지가 없다.

**모델별 예외는 어떻게 되는가**

| | 일반화 (`core/preprocess.py`) | 예외 (`spec.json`) |
| --- | --- | --- |
| 리사이즈 | 6종 구현 | 어느 종류인가 |
| 배수 맞추기 | 올림/내림/반올림/제약 4종 | 어느 규칙인가 (같은 4:3 사진이 700 이 되기도 686 이 되기도 한다) |
| 정규화 | 5종 구현 | 어느 종류, 어떤 평균·표준편차 |
| 여백 채우기 | 구현 | 검정인가 흰색인가 |
| 리사이즈 시점 | uint8 먼저 / float 먼저 | 어느 쪽인가 (보간 넘침 처리가 달라 결과가 바뀐다) |
| 좌표 되돌리기 | `Geometry` 로 구현 | — |

**규모**: 12개 모델, 모델당 30분~1시간. **GPU 불필요.**

---

## 6. 속도와 무관하지만 남아 있는 것

| | 내용 | 판단 |
| --- | --- | --- |
| **시각화 데모** | 포인트 클라우드 + 2D 여러 방식. 설계는 `REFACTOR_PLAN.md` §11 | 속도 작업 뒤. 단 **최적화의 안전장치이기도 하다** — "이 정도 차이가 괜찮은가"는 숫자만으로 판단할 수 없다 |
| **테스트 이미지 확대** | 지금 `data/example.jpg` **한 장뿐** | **종횡비가 다른 사진 한 장은 지금 필요하다** — 해상도 실험을 4:3 한 장으로만 검증할 수 없다 |
| **VGGT 3분할 포팅** | 업스트림 변경으로 export 불가 (`docs/model_contracts.md` D19) | 보류. 단일 경로가 정상 동작하고 3분할이 더 빠르다는 근거도 없다 |
| **리사이즈 민감도 표** | 기존 6행의 기준선을 재구성할 수 없다 | **S2 로 흡수된다** — "우리가 얼마나 손해 봤나"에서 "더 줄여도 되나"로 성격이 바뀌었다 |

---

## 7. 작업 오케스트레이션

### 7.1 제약 조건 (실측)

| 제약 | 내용 |
| --- | --- |
| 기계가 둘 | 노트북(GPU 없음, numpy·cv2 있음) / 데스크탑(RTX 3080, conda 환경 12개) |
| **GPU 는 직렬 자원** | 빌드도 측정도 한 번에 하나 |
| **측정이 세션마다 ~2% 흔들린다** | 같은 엔진을 다시 재서 4.23 → 4.31 ms. 한 런 내부 표준편차는 0.12 ms 뿐 |
| 빌드가 길다 | VGGT 5분, depth_pro 15분 이상 |
| ssh 가 끊겨도 원격 작업은 살아남는다 | 다만 로그 파일이 잠긴 채 남아 **재실행이 막힌다** |
| 결과 파일은 데스크탑이 쓰고 노트북이 커밋한다 | 회수 전 git 조작 금지 — `tools/sync_desktop.sh` 가 강제 |

### 7.2 원칙 여섯 개

**1. 빌드와 측정을 분리한다** — 가장 중요하다

빌드는 **무인·야간·순서 무관**, 측정은 **유휴 GPU 에서 연속 한 번에.**

이유가 위의 2% 흔들림이다. 모델마다 "빌드하고 바로 재고 다음으로" 넘어가면
12개 숫자가 12개 다른 시점에 측정되어 **그 흔들림이 모델 간 차이에 섞인다.**

```
잘못:  모델1 빌드→측정, 모델2 빌드→측정, ...
맞음:  전부 빌드 (몇 시간, 무인) → 전부 한 번에 연속 측정
```

**속도 작업에서는 특히 치명적이다.** S1~S5 의 이득 중 상당수가 한 자릿수
퍼센트인데, 2% 흔들림에 묻히면 "빨라졌는지" 자체를 판단할 수 없다.

**2. 모델이 아니라 축으로 묶는다** — 16비트 12개 전부 빌드 → 24개를 한 번에 측정

**3. 일은 있어야 할 곳에서**

| 노트북 (GPU 불필요) | 데스크탑 (GPU 필요) |
| --- | --- |
| 전처리 일반화 + 바이트 대조 | export |
| spec 편집·검증 | 엔진 빌드 |
| 시각화 코드 | 측정 |
| 문서·테스트 | 정확도 검증 |

이 경계는 이미 테스트가 강제한다 — `core/spec.py` 와 `core/artifact.py` 는
torch·TensorRT 를 import 하지 않는다.

**4. GPU 없이 확인 가능한 것은 전부 GPU 없이**

**5. 측정 중에는 GPU 를 독점한다** — 빌드가 도는 중에 잰 숫자는 버려야 한다

**6. 긴 작업은 끊겨도 살아남게 만든다**

### 7.3 엔진을 바꾸는 결정을 먼저 한다

| 결정 | 영향 |
| --- | --- |
| 인코더 크기 변경 | **해당 모델 재export·재빌드·재측정** |
| 해상도 변경 | 동일 |
| 정밀도 변경 | 재빌드 |
| uint8 적용 | 엔진 교체 |

**전부 S6(72회 빌드) 전에 끝내야 한다.** 안 그러면 스윕이 통째로 버려진다.

### 7.4 병렬화

| | 판단 |
| --- | --- |
| 빌드 여러 개 동시에 | **하지 않는다.** 3080 은 10GB 이고 depth_pro·VGGT 빌드가 메모리를 크게 쓴다 |
| 노트북 작업과 데스크탑 작업 병행 | **한다.** Q3(전처리)와 S1~S4 는 독립 |
| export(CPU 위주)와 빌드(GPU) 겹치기 | 권장하지 않는다 — 얻는 것에 비해 실패 원인이 는다 |
| 타이밍 캐시 재사용 | **한다.** 스윕 빌드 시간을 크게 줄인다 |

### 7.5 필요한 도구 하나

**데스크탑에서 긴 작업 여러 개를 순서대로 돌리고, 연결이 끊겨도 살아남고,
어디까지 갔는지 남기는 것.** 분산 시스템이 아니라 **스크립트 하나**다.

- 작업 목록 파일 / 순차 실행 / 작업별 로그 / 진행 상태 파일
- **측정 중 빌드 금지 (잠금)**

`run.py` 가 이미 "어느 환경에서 무엇을 실행할지"를 알고 있으므로 여기에 얹는다.

---

## 8. 착수 순서

```
[전제 — 이것 없이는 속도를 밀 수 없다]
  Q2 변형 여러 개 다루기 (반나절, 노트북)
  Q1 정답 데이터 최소 배선 (1~2일)        ← 품질 하한선을 잴 수단
  Q3 전처리 일반화 (12개, 노트북)          ← 해상도를 바꾸려면 필요

[속도 — 큰 것부터]
  S1 metric3d_v2 (16비트 + 작은 인코더)    349.8 → 60~80 ms 기대
  S2 해상도 낮추기 (depth_pro 우선)        240.5 → 30~40 ms 가능성
  S3 인코더 작은 급 (dav3, unidepth_v2, unik3d)
  S4 그래프 정리 (64비트 12개 전수 + 단순화 통일)
  S5 uint8 전 모델 적용
  → 여기서 전부 빌드 → 한 번에 측정 → 정답 대비 품질 확인

[짜내기]
  S6 최적화 레벨 스윕 (72회, 무인)
  S7 보류 항목 재검토
```

| | 작업 | 기대 | 어디서 | 선행 |
| --- | --- | --- | --- | --- |
| **1** | **Q2 변형 여러 개 다루기** | — (전제) | 노트북 | — |
| **2** | **Q1 정답 데이터 최소 배선** | — (전제) | 양쪽 | Q3 일부 |
| **3** | **Q3 전처리 일반화** | — (전제) | 노트북 | — |
| **4** | **S1 metric3d_v2** | **4~6배** | 데스크탑 | 1·2 |
| **5** | **S2 해상도** | **최대 6배** (depth_pro) | 데스크탑 | 2·3 |
| **6** | **S3 인코더** | 2~3배 (3개 모델) | 데스크탑 | 1·2 |
| **7** | **S4 그래프 정리** | 최대 20% | 데스크탑 | 1 |
| **8** | **S5 uint8** | 모델당 0.6~0.8 ms | 데스크탑 | 1 |
| 9 | S6 최적화 레벨 스윕 | 0~10% | 무인 | 4~8 완료 |
| 10 | 시각화 데모 | — | 양쪽 | 3 |

**1·3 은 GPU 가 필요 없고 4~8 을 전부 막고 있다.** 여기서 시작한다.
**2 는 4~6 을 하기 전에 최소한만이라도 있어야 한다** — 없으면 품질이 얼마나
떨어졌는지 모른 채 되돌릴지 말지를 판단하게 된다.

---

## 9. 판단이 필요한 것

| | 내용 | 늦추면 |
| --- | --- | --- |
| **품질 하한선** | "이 정도까지는 나빠져도 된다"의 기준. 정답 데이터가 갖춰지면 숫자로 정할 수 있다 | S1·S2·S3 를 되돌릴지 판단할 근거가 없어진다 |
| **정답 데이터 범위** | 실내만 / +실외 / +합성 | 실내부터 시작하면 늦어도 무방 |
| **`depth_pro` 해상도** | 1536 을 낮춰볼 것인가 (무너질 수 있다) | 저장소 최대 병목 하나가 그대로 남는다 |
| **테스트 이미지** | 어떤 장면. **종횡비가 다른 것 최소 1장은 지금 필요** | 해상도 실험을 4:3 한 장으로 검증하게 된다 |
| 3D 뷰어 | 파일 저장 / open3d 창 / 브라우저 | 시각화 착수 시점 |

---

## 10. 지켜온 원칙 (계속 지킨다)

- **수치는 손으로 적지 않는다.** 측정 → 파일 → 표 자동 생성
- **재현 조건을 함께 적는다.** 입력 크기·정밀도·인코더·GPU·드라이버·버전
- **검증 안 된 것을 완료로 표시하지 않는다**
- **하나의 순위로 줄 세우지 않는다.** 입력 크기가 다르면 같은 줄에 놓지 않는다
- **자동으로 조정하지 않는다.** 빌드 설정을 코드가 알아서 바꾸면 비교가 깨진다
- **못 한 것은 못 했다고 적는다.** 이유를 모르면 "이유를 모른다"고 적는다
- **2% 보다 작은 차이는 이득이라 부르지 않는다.** 세션 간 흔들림이 그만큼이다
