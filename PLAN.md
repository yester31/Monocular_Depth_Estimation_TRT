# 작업 계획 — 정확도 기반 추론 최적화

갱신: 2026-08-14

대상 브랜치: `refactor/planning`

이 문서는 **앞으로 실행할 작업의 단일 계획서**다. 완료된 리팩토링의 경위는
`REFACTOR_PLAN.md`, 과거 정리 제안은 `CLEANUP_REVIEW.md`에 남아 있지만, 두 문서의
오래된 “미완” 표시는 현재 작업 목록으로 사용하지 않는다.

측정값의 단일 출처:

- 속도: `reports/bench/`, `reports/comparison.md`
- **정답 데이터 정확도: `reports/gt/`, `reports/gt.md`**
- **평가에 쓴 이미지 목록과 체크섬: `data/eval/*.json`**
- 엔진 대 ONNX 정확도: `reports/accuracy.json`, `reports/accuracy.md`
- 레이어 프로파일: `reports/profile/`
- 빌드 조건: 각 `<engine>.buildinfo.json`
- MoGe 내보내기 A/B: `reports/tune/moge_ab/`
- 모델 계약과 환경: 각 `models/<name>/spec.json`, `python models.py`

상태 표기: `완료` · `진행 가능` · `결정 필요` · `보류` · `외부 제약`

---

## 1. 목표와 완료 정의

목표는 14개 모델을 같은 조건에서 비교하면서, 속도를 올려도 출력 의미와 품질을
잃지 않는 것이다. 전체 계획의 완료 조건은 다음 다섯 가지다.

1. 서로 다른 종횡비의 입력과 정답 깊이 데이터로 평가할 수 있다. — **정답 데이터 완료, 종횡비 확장 남음**
2. 각 모델의 출력 계약(metric/relative/geometry)에 맞는 지표를 사용한다. — **완료**
3. fp16과 fp32의 직접 비교 결과가 있고, 정밀도 선택 근거가 남아 있다. — **완료**
4. 채택한 최적화는 독립 빌드 3회, 정확도, 재현성 검증을 모두 통과한다.
5. 결과와 환경이 JSON에 기록되고 Markdown 표는 JSON에서 생성된다.

**정답 데이터 평가가 준비되기 전에는 인코더·해상도·INT8처럼 모델 품질을 바꿀
수 있는 변경을 채택하지 않는다.**

---

## 2. 현재 기준선

**모델 14개**, 전부 클럭 1800 MHz 고정에서 빌드·측정됨. 속도는
`reports/comparison.md`, 정답 데이터 정확도는 `reports/gt.md`.

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| GPU 클럭 1800 MHz 고정 정책 | 완료 | 빌드·측정 동일 조건. 빌드 전후 클럭을 `<engine>.buildinfo.json` 에 기록하고, 흔들렸으면 로드할 때마다 경고 |
| 14개 엔진 빌드·측정 | 완료 | `reports/comparison.md`. 표에 `fp32` · `moved` 열이 있어 엔진 상태가 속도 옆에 보인다 |
| 14개 엔진 대 ONNX 정확도 | 완료 | 실패 0. `depth_anything_v3` 하늘 마스크만 WARN (값 크기 6.57e-06 이라 상대오차가 부풀려짐, 상관 0.998) |
| 14개 전수 레이어 프로파일 | 완료 | `reports/profile/`. 프로파일에도 클럭과 엔진 스탬프가 들어가 벤치 기록과 짝이 맞는지 검사됨 |
| **정답 데이터 평가 (DIODE 실내 50장)** | **완료 — 11개 모델** | `reports/gt.md`. 어댑터는 전부 `reports/inputs/<model>.npy` 와 대조해 통과한 것만 채점 |
| **`unidepth_v2` · `unik3d` 672x896 재빌드** | **완료** | D11 의 3.1배 스케일 오차가 1.08 · 1.15 로 내려감. 모델 성질이 아니라 518 을 강제한 대가였다 |
| **`metric3d_v2` canonical → 미터** | **완료** | DIODE 가 전역 캘리브레이션을 제공(`fx=886.81`). "영구 차단" 으로 적혀 있었으나 임의 사진에 대해서만 참이었다 |
| **DIODE 깊이 규약** | **z-depth 로 측정 확인** | `tools/check_diode_convention.py`. 문서 어디에도 없어서 평면 피팅으로 판정 |
| **MoGe shift solver 재구현** | 완료 | `core/pointmap.py`. 원본과 최대 3.6e-05 일치(`tools/check_pointmap.py`). 평가가 모델 클론 없이 돈다 |
| 최소 인코더 적용 | 완료 | 제공 선택지가 있는 모델 전부 |
| MoGe 내보내기 방식 4종 A/B | 완료 | 12개 독립 빌드, 현재 Dynamo+단순화 유지 |
| `moge_2` · `metric_anything` 빌더 튜닝 | 완료 — 채택 없음 | 내보내기 4종 · 최적화 레벨 0~5 · 워크스페이스 2/4/8 GiB, 총 27빌드. 전부 무효 |
| `moge_2` 이동 비용 32% | 구조적, 종결 | `replicate` 패딩. ONNX 노드를 2282→633 으로 줄여도 불변 |
| `tr2m` · `zipdepth` 추가 | 완료 | 13번째·14번째 모델 |

현재 기준선은 보존한다. 실험 ONNX 와 엔진은 별도 이름·별도 결과 경로를 쓰며,
채택 결정 전에는 `reports/bench` 나 발표용 엔진을 교체하지 않는다.

---

## 3. 실행 순서와 의존성

| 순서 | 작업 패키지 | 의존성 | 상태 |
| ---: | --- | --- | --- |
| ~~1~~ | ~~P0 측정 메타데이터 완성~~ | — | **완료** — 클럭·엔진 스탬프·fp32/이동 비중이 결과 JSON 과 비교표에 |
| 2 | **P1 공통 전처리를 실제 경로에 연결** | 없음 | 진행 가능 |
| ~~3~~ | ~~P2 평가 입력 세트 확장~~ | — | **부분 완료** — DIODE(MIT) 실내 50장. 16:9·세로는 남음 |
| ~~4~~ | ~~P3 정답 데이터 평가 체계~~ | — | **완료 — 11개 모델** |
| ~~5~~ | ~~P4 fp16 대 fp32 직접 A/B~~ | — | **완료 — 8 측정 / 2 빌드 불가** |
| 6 | **P5 보류된 변경 채택 결정** | — | **결정 필요 — 다음 작업** |
| 7 | **P6 빌더 최적화 레벨 스윕** | P4, P5 | 대기 |
| 8 | **P7 통합 시각화 데모** | P1 | 대기 |

P3 가 P1 보다 먼저 끝난 것은 순서를 어긴 것이 아니다. P1 의 역변환이 필요한 것은
전처리가 pad/crop 인 모델뿐이고, 채점된 11개는 stretch 이거나 자기 pad 를 스스로
되돌린다. 남은 3개(`vggt`, `streamvggt`, `tr2m`)가 P1 을 기다린다.

**다음은 P5 다.** fp16 근거는 P4 가 만들었다(§8). 남은 것은 보류해 둔 변경들을
채택할지 결정하는 것이고, 이제 그 결정을 정답 데이터로 검증할 수 있다.

---

## 4. P0 — 측정 메타데이터 완성 · **완료**

### 목적

낡거나 잘못 빌드된 엔진을 일반 벤치마크 결과만 보고도 발견할 수 있게 한다.

### 작업

1. 일반 벤치마크가 끝난 뒤 별도의 짧은 프로파일 구간을 실행한다.
2. 프로파일러는 timed loop에 붙이지 않는다. 레이어 프로파일은 실행을 직렬화하므로
   같은 구간에서 측정하면 발표용 latency가 오염된다.
3. 각 결과 JSON에 다음을 기록한다.
   - `clock_mhz`, `clock_max_mhz`
   - fp32 출력 레이어 시간 비중
   - Reformat/Slice 등 데이터 이동 시간 비중
   - 엔진 레이어 수와 정밀도 구성
4. 이전 JSON을 읽는 코드와 표 생성기는 새 필드가 없더라도 동작하게 한다.

### 완료 조건

- 12개 결과 모두 환경과 프로파일 요약 필드를 가진다.
- 발표용 latency는 프로파일 기능 추가 전후 1% 이내에서 재현된다.
- `tests/test_bench.py`, `tests/test_profiles.py`, `compare.py --check`가 통과한다.

---

## 5. P1 — 공통 전처리를 실제 추론·평가 경로에 연결

### 목적

`core/preprocess.py`를 테스트 전용 코드가 아니라 평가와 시각화가 공유하는 실제
구현으로 만든다. ONNX의 고정 입력 크기나 모델별 수학은 바꾸지 않는다.

### 작업 순서

1. 12개 모델의 현재 전처리 호출부와 `spec.json` 선언을 대조한다.
2. canary 모델을 유형별로 하나씩 선정해 공통 전처리로 연결한다.
   - stretch
   - keep-ratio resize
   - keep-ratio pad
   - square crop/pad
3. 기존 코드와 공통 코드에 같은 uint8 이미지를 넣고 입력 tensor와 geometry를
   비교한다.
4. canary가 통과한 유형부터 같은 유형의 나머지 모델로 확장한다.
5. `run.py infer`와 이후 평가·시각화 경로가 같은 함수를 사용하게 한다.

### 완료 조건

- 12개 모델 모두 전처리 유형을 `spec.json`에서 결정한다.
- **`reports/inputs/<model>.npy` 가 바이트 단위로 동일하다.** 허용오차가 아니다.
  이 파일은 `verify_accuracy.py` 가 엔진과 ONNX 를 비교할 때 쓰는 기준 입력이라,
  여기가 조금이라도 움직이면 12개 정확도 기준선이 전부 소리 없이 이동하고 이후
  비교가 무엇과의 비교인지 알 수 없게 된다. 같은 수학이므로 바이트 동일이
  가능하고, 안 되면 그건 전처리가 실제로 바뀐 것이다.
- 좌표 복원(resize/pad 역변환)이 기존과 일치한다.
- 4:3, 16:9, 정사각형 입력 테스트가 통과한다.
- `tests/test_preprocess.py`가 표준 테스트 실행에 포함되거나, 선택 의존성 누락 시
  명시적으로 skip한다.
- 기존 ONNX·엔진·발표용 벤치마크는 바뀌지 않는다.

### 중단 조건

한 모델이라도 입력 tensor 또는 좌표 계약이 달라지면 전체 일괄 전환을 멈추고 그
모델을 기존 경로에 남긴다. 차이를 “사소한 전처리 변경”으로 간주하지 않는다.

---

## 6. P2 — 평가 입력 세트 확장

### 필요한 결정 D1

평가 이미지를 사용자가 제공할지, 라이선스가 명확한 외부 데이터에서 가져올지
정해야 한다. 출처를 확인할 수 없는 이미지는 저장소에 추가하지 않는다.

### 최소 입력 구성

| 유형 | 최소 수량 | 용도 |
| --- | ---: | --- |
| 4:3 landscape | 1 | 현재 MoGe/Metric Anything 기준 유지 |
| 16:9 landscape | 1 | 종횡비 변화 검증 |
| portrait 또는 near-square | 1 | pad/resize 좌표 검증 |
| 실내 정답 깊이 | 1 scene 이상 | metric 품질 평가 |

각 입력에는 출처, 라이선스, 원본 크기, SHA-256을 manifest로 기록한다.

### 완료 조건

- 최소 세 종횡비가 자동 테스트와 시각화에 사용된다.
- 모든 파일의 사용 권한과 출처가 문서화된다.
- 테스트 입력과 정답 데이터는 별도 manifest에서 구분된다.

---

## 7. P3 — 정답 데이터 평가 체계 · **완료 (11/14)**

### 결과

`reports/gt.md`. DIODE 실내 50장, 미터, z-depth, 스캐너 마스크 적용.

| 모델 | 정렬 | AbsRel | δ1 | 원하는 배율 |
| --- | --- | ---: | ---: | ---: |
| `depth_anything_ac` | scale+shift | 5.44% | 96.6% | — |
| `distill_any_depth` | scale+shift | 5.96% | 95.9% | — |
| `zipdepth` | scale+shift | 7.93% | 93.3% | — |
| `metric3d_v2` | none | 13.10% | 86.6% | 1.000 |
| `metric_anything` | none | 15.63% | 75.4% | 1.123 |
| `depth_pro` | none | 17.48% | 72.7% | 1.012 |
| `unidepth_v2` | none | 18.22% | 67.8% | 1.081 |
| `moge_2` | none | 21.39% | 52.1% | 1.192 |
| `unik3d` | none | 22.42% | 56.1% | 1.146 |
| `depth_anything_v2` | none | 23.06% | 52.0% | 1.141 |
| `depth_anything_v3` | none | 60.87% | 0.2% | 2.609 |

**위아래는 순위가 아니다.** 위 3개는 자기가 주장하는 만큼(scale+shift)을 맞춘 뒤,
아래는 나온 미터 그대로 채점했다. 세로로 읽는 것이 이 표가 유도하는 유일한 오독이라
정렬 방식을 표 안에 넣었다.

**마지막 열은 진단이지 점수가 아니다.** 모두에게 같은 정렬을 허용했을 때 전 모델이
4.4%~6.5% 안에 들어온다. 미터에서 갈리는 것은 보는 능력이 아니라 보정이다.
`depth_anything_v3` 의 2.609 만 품질이 아니라 단위 문제다.

**채점되지 않은 3개:** `vggt` · `streamvggt` 는 출력이 정규화 좌표계라
(장면 점들의 평균 거리 = 1, 논문 본문 확인) metric 순위에 넣지 않는다 — scale 정렬로는
평가 가능하며 P1 뒤에 별도 표로 낸다. `tr2m` 은 프롬프트가 이미지마다 필요해 평가
계약이 다르다.

### 원래 계획

### 목적

현재 가장 큰 위험인 “속도는 빨라졌지만 품질 손실을 모른다”를 닫는다.

### 데이터 계약

평가 manifest는 최소한 다음 정보를 가진다.

- RGB 경로와 원본 크기
- depth 경로, 단위, invalid 값
- valid mask
- 카메라 intrinsics가 있으면 그 값과 좌표계
- 평가 crop/resize 규칙

### 출력별 평가 규칙

| 출력 계약 | 평가 방식 |
| --- | --- |
| metric depth | 스케일 정렬 없이 AbsRel, RMSE, δ1 |
| relative depth | 정렬 전 결과와 명시된 scale/shift 정렬 후 결과를 분리 보고 |
| canonical/point map | 해당 모델 계약에 맞는 변환 후 평가; 변환을 못 검증하면 순위 제외 |
| VGGT/StreamVGGT geometry | 출력 의미가 확정되기 전 metric 순위에 넣지 않음 |

### 완료 조건

- 같은 이미지·mask·평가 해상도로 12개 모델을 실행한다.
- 각 모델은 수치 결과 또는 명시적인 `unsupported` 이유를 남긴다.
- `metric3d_v2`, `unidepth_v2`, `unik3d`의 이전/현재 인코더 품질 차이를 측정한다.
- 평가 보고서는 JSON에서 자동 생성한다.
- 수동으로 골라낸 “좋아 보이는” 이미지만 보고 결론내리지 않는다.

---

## 8. P4 — fp16 대 fp32 직접 A/B · **완료 (8 측정 / 2 빌드 불가)**

### 결과 — fp16 은 정확도에서 측정되지 않는다

같은 ONNX, 같은 빌드 옵션, 정밀도만 다른 엔진. 정확도는 DIODE 실내 50장 정답 데이터.

| model | AbsRel fp16 | AbsRel fp32 | 차이 | δ1 fp16 | δ1 fp32 | ms fp16 | ms fp32 | 비용 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `zipdepth` | 7.93% | 7.93% | −0.00p | 93.3% | 93.3% | 2.77 | 3.30 | **1.19×** |
| `moge_2` | 21.39% | 21.40% | +0.01p | 52.1% | 52.0% | 19.73 | 41.90 | **2.12×** |
| `depth_anything_ac` | 5.44% | 5.43% | −0.01p | 96.6% | 96.6% | 4.38 | 11.31 | **2.58×** |
| `depth_anything_v2` | 23.06% | 23.07% | +0.01p | 52.0% | 52.0% | 4.30 | 11.33 | **2.63×** |
| `distill_any_depth` | 5.96% | 5.95% | −0.00p | 95.9% | 95.9% | 4.32 | 11.38 | **2.63×** |
| `depth_pro` | 17.48% | 17.52% | +0.04p | 72.7% | 73.2% | 242.12 | 874.54 | **3.61×** |
| `depth_anything_v3` | 60.87% | 60.88% | +0.01p | 0.2% | 0.2% | 19.90 | 74.32 | **3.74×** |
| `metric_anything` | 15.63% | 15.67% | +0.04p | 75.4% | 75.1% | 67.00 | 304.05 | **4.54×** |

**여덟 모델 전부, 최대 차이 0.04%p.** 방향도 양쪽으로 갈리므로 부호에 의미가 없다.
AbsRel 5%~61%, 실행 시간 2.8ms~242ms 에 걸쳐 있는데도 정답 데이터가 정밀도를
구분하지 못한다. `depth_pro` 는 fp32 에서 δ1 이 오히려 0.5%p **떨어졌다**.

**이것이 이 저장소에서 fp16 을 쓰는 첫 번째 측정 근거다.** 기존 정확도 검증은
엔진과 그 엔진을 만든 ONNX 를 비교하므로, fp32 엔진과 fp16 엔진을 서로 다른 정답에
대해 똑같이 충실하다고 보고한다 — 정밀도 자체의 손해는 원리상 볼 수 없다.

### fp32 로는 빌드가 안 되는 모델이 둘 있다

`unidepth_v2` 와 `unik3d` 는 672x896 fp32 에서 `build_serialized_network` 가 None 을
반환한다. `unidepth_v2` 는 워크스페이스를 1 GiB 로 낮춰도 실패했으므로 워크스페이스가
아니라 RTX 3080 10 GB 에 fp32 가중치와 활성값이 들어가지 않는 것으로 보인다.

**이 둘에게 fp16 은 선택이 아니라 조건이다.** 성공으로 세지 않고 표에서 숨기지도
않는다.

### 아키텍처가 비용을 정한다

`zipdepth` 의 **1.19배**가 눈에 띈다. 나머지는 2.1~4.5배인데 이 모델만 fp32 가
거의 공짜다. 저장소에서 유일한 컨볼루션 모델(6.1M)이고, 텐서코어보다 대역폭에
묶여 있어서 fp16 이 속도로 사줄 것이 적다. **이 모델에서는 정밀도 선택이 거의
무의미하고, `metric_anything` 에서는 4.5배짜리 결정이다.**

### 남은 것

`vggt` · `streamvggt` · `tr2m` 은 아직 fp32 엔진이 없다(정답 데이터 평가도 아직).
`metric3d_v2` 는 이미 fp32 라 비교 대상이 아니다.

### 원래 계획

### 범위

현재 fp16을 사용하는 모델마다 같은 ONNX와 빌드 옵션으로 fp32 엔진을 별도
생성한다. `metric3d_v2`는 현재 의도적으로 fp32이므로 새 fp32 비교 대상이 아니다.
따라서 신규 fp32 엔진은 최대 11개다. VRAM이나 TensorRT 제약으로 빌드할 수 없는
모델은 실패 원인을 기록하고 성공으로 세지 않는다.

### 비교

1. fp16 TensorRT 대 fp32 TensorRT — 정밀도 선택의 직접 영향
2. 두 엔진 각각 대 fp32 ONNX — TensorRT 변환 오차
3. 정답 데이터 지표 — 실제 품질 영향
4. latency, 엔진 크기, VRAM, fp32 fallback 비중

### 완료 조건

- 성공한 모든 모델에 동일 입력의 출력별 오차와 정답 데이터 지표가 있다.
- 속도만으로 fp16을 채택하지 않는다.
- fp32 엔진 빌드 실패와 평가 미지원 모델을 결과표에서 숨기지 않는다.
- 기본 엔진 변경은 별도 결정으로 남긴다.

---

## 9. P5 — 보류된 변경의 결정 게이트

### D2. uint8 입력 · **결정: 변이(variant)로 채택, 기준선은 유지**

정답 데이터로 판정할 수 있게 되어 채택 조건을 실제로 확인했다.

| 모델 | 입력 MB f32 → u8 | h2d ms | 총 ms | 속도 | AbsRel f16 → u8 | δ1 |
| --- | --- | --- | --- | ---: | --- | --- |
| `depth_anything_v2` | 3.22 → 0.80 | 1.034 → 0.269 | 4.29 → 3.40 | **−20.7%** | 23.06% → 23.07% (+0.01p) | 52.0% → 51.9% |
| `vggt` | 3.22 → 0.80 | 1.048 → 0.276 | 51.97 → 50.96 | −1.9% | 미측정 |  |
| `depth_pro` | 28.31 → 7.08 | 9.056 → 2.287 | 242.19 → 238.39 | −1.6% | 17.48% → 17.55% (+0.07p) | 72.7% → 72.7% |

**품질 저하는 fp16 대 fp32 와 같은 잡음 대역 안이다** (+0.01p, +0.07p).

**핵심은 절감이 비율이 아니라 절대량이라는 것이다.** 세 모델 모두 h2d 가 입력
크기에 비례해 줄었고 — PCIe 대역폭이지 모델의 성질이 아니다 — 남은 비율은 순전히
연산 시간이 얼마나 큰가로 정해진다. `depth_pro` 는 **절대 절감이 가장 크지만**
(6.8 ms) 242 ms 짜리 모델이라 1.6% 로 보이고, `depth_anything_v2` 는 절감이
0.77 ms 인데 4.3 ms 짜리라 21% 가 된다.

**결정**

1. **float32 NCHW 를 기준선으로 유지한다.** 비교표의 행들이 같은 입력 계약을
   가져야 하고, uint8 은 전처리 일부를 그래프 안으로 옮기므로 "모델의 속도" 에
   하네스의 선택이 섞인다.
2. **`depth_anything_v2` uint8 NHWC 를 명시적 변이로 추가한다.** 21% 를 공짜로
   얻고 품질은 잡음 안이다.
3. **느린 모델에는 적용하지 않는다.** `depth_pro` · `vggt` 는 2% 미만이라 입력
   계약을 바꿀 이유가 없다.
4. 판단 기준은 모델별로: **입력 MB × 약 0.24 ms/MB 가 연산 시간에 비해 큰가.**

### D3. 리사이즈 민감도 표

기존 일부 행의 기준선을 코드에서 재구성할 수 없다. 다음 중 하나를 정해야 한다.

1. 기존 표를 역사 기록으로 남기고 전 모델을 새 도구로 다시 측정한다. **권장**
2. 기존 표와 새 3행을 분리해 서로 비교하지 않는다.

새 측정은 입력, 기준 출력, 정렬 방식, mask, seed를 JSON에 기록해야 한다.

### D4. Phase 6 범위

다음 단계는 자동으로 시작하지 않는다. 별도 승인 후 범위를 하나씩 연다.

- `later/` 16개 프로젝트
- CPU 추론 백엔드
- BF16
- INT8
- CUDA Graph

---

## 10. P6 — 빌더 최적화 레벨 스윕

### 선행 조건

P0, P3, P4가 완료되고 P5의 기본 엔진 형태가 확정되어야 한다. 정확도 기준선 없이
72개 엔진부터 만드는 것은 금지한다.

### 실행 프로토콜

1. GPU 클럭을 1800 MHz로 고정하고 시작·종료·각 결과에 클럭을 기록한다.
2. 12개 모델 × 레벨 0~5의 72개 후보를 기준선과 분리된 경로에 빌드한다.
3. 첫 빌드는 **스크리닝일 뿐 채택 근거가 아니다.**
4. 기본 레벨과 유력 후보를 각각 총 3회 독립 빌드한다.
5. 회차마다 후보 순서를 바꾸고, 엔진·타이밍 캐시를 공유하지 않는다.
6. 각 엔진의 정확도와 fp32/Reformat/Slice 비중을 확인한다.

### 채택 조건

- 평균 latency 개선이 2% 이상이다.
- 3개 빌드의 범위와 개별 결과를 함께 공개한다.
- 기본값과 후보 분포가 겹치거나 후보 편차가 더 크면 채택하지 않는다.
- 정확도 판정 실패가 없고 정답 데이터 품질이 기준선 안이다.
- 모델별 근거가 있을 때만 적용하며 전 모델에 같은 레벨을 강제하지 않는다.

`moge_2` 레벨 5처럼 한 번 빠르고 다음 빌드가 크게 느려지는 설정은 평균만 좋아도
채택하지 않는다.

---

## 11. P7 — 통합 시각화 데모

### 범위

- 동일 입력의 12개 결과를 나란히 표시
- metric depth는 공통 단위/범위, relative depth는 정렬 여부 표시
- depth, mask, normal, confidence, camera/FOV 등 실제 출력만 표시
- 지원되는 모델의 point cloud
- 원본 크기 복원과 pad/crop 영역 표시

### 완료 조건

- 결과 이미지를 개별 min/max로 정규화해 metric 차이를 숨기지 않는다.
- 모델명, 입력 크기, 출력 의미, 단위, 정밀도, 엔진 지문을 함께 표시한다.
- headless 저장 모드가 있고 GUI가 없어도 테스트할 수 있다.
- 최소 세 종횡비에서 레이아웃과 좌표 복원을 확인한다.

루트 `demo.py`는 이 단계에서 만든다. 현재 각 모델의 `onnx2trt.py`와
`run.py infer`가 제공하는 기능을 먼저 공통 API로 묶은 뒤 UI를 얹는다.

---

## 12. 보류·외부 제약

| 항목 | 상태 | 재개 조건 |
| --- | --- | --- |
| MoGe 메시 출력 | 보류 | 고정 `utils3d`에 맞춘 포팅 + 실제 GLB 시각 검증 |
| VGGT split 엔진 | 외부 제약 | 현재 업스트림 export 문제가 해결되거나 호환 패치 검증 |
| BF16 | 보류 | P3/P4 완료 후 fp16 대비 목적과 기준 합의 |
| INT8 | 보류 | 교정 데이터와 정확도 허용치 합의 |
| CUDA Graph | 보류 | 측정 상한 약 1.5%보다 우선순위 높은 작업 완료 |
| 해상도 축소 | 보류 | 최적화가 아니라 품질 변경이므로 P3 평가 후 별도 실험 |
| 추가 인코더 축소 | 종료 | 12개 모두 업스트림 제공 최소 또는 선택지 없음 |

---

## 13. 실행 규칙

0. **GPU 클럭을 고정하지 않고 빌드하지 않는다** (`nvidia-smi -lgc 1800,1800`,
   해제는 `-rgc`). TensorRT 는 빌드 중 GPU 에서 커널 후보를 실제로 재보고
   고르므로, 클럭이 흔들리면 커널 선택이 부분적으로 난수가 된다. 같은 ONNX·같은
   옵션·지문 바이트 동일인 unik3d 빌드 4회가 19.04 / 13.35 / 8.41 / 14.49 ms 였고,
   클럭 고정 후 3회가 9.66 / 9.62 / 9.68 이었다. 엔진마다
   `<engine>.buildinfo.json` 에 빌드 전후 클럭이 기록되고, 두 값이 다르면
   `common.get_engine()` 이 로드할 때마다 경고한다. **경고가 뜨는 엔진의 속도는
   그 모델의 속도가 아니다.**
1. GPU 빌드와 측정은 병렬 실행하지 않는다.
2. 원격 작업은 실제 프로세스·로그·결과 파일을 확인하기 전 “실행 중” 또는 “완료”로
   보고하지 않는다. SSH 세션에서 분리한 프로세스가 살아남는다고 가정하지 않는다.
3. 원격 코드를 갱신하기 전에 측정 결과를 먼저 회수한다.
4. 기존 엔진을 덮어쓰지 않고 variant별 파일명을 사용한다.
5. 한 번의 빌드나 2% 미만 차이를 이득으로 발표하지 않는다.
6. 속도·정확도·재현성 중 하나라도 없으면 채택하지 않는다.
7. 수치는 손으로 표에 옮기지 않고 JSON에서 생성한다.
8. 입력 크기나 출력 의미가 다른 모델을 같은 순위로 오해하게 만들지 않는다.
9. 실패·OOM·unsupported를 결과에서 삭제하지 않는다.
10. 변경 후 최소 검증:
    - `python -m py_compile <changed python files>`
    - `git diff --check`
    - 관련 단위 테스트
    - 전체 `tests/` 회귀 테스트
    - GPU 변경이면 엔진 정확도와 반복 측정

---

## 14. 다음 실행 묶음

다음 작업은 사용자 결정 없이 안전하게 시작할 수 있는 범위다.

1. P0 결과 JSON 스키마와 테스트부터 추가한다.
2. timed loop 밖의 짧은 프로파일 수집기를 공통 코드에 연결한다.
3. P1 전처리 호출부를 전수 조사하고 유형별 canary를 정한다.
4. canary의 tensor·geometry parity 테스트를 만든다.
5. 12개로 확대하기 전에 차이와 예외를 보고한다.

그다음 P2의 이미지 출처와 P3의 정답 데이터셋 선택에서 사용자 결정을 받는다.
