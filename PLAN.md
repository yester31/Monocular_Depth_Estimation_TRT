# 작업 계획 — 추론 속도 기준

2026-08-13. `REFACTOR_PLAN.md`는 끝난 리팩토링의 기록, 이 문서는 앞으로 할 일.
측정 수치는 여기 적지 않는다 — `reports/comparison.md`, `reports/accuracy.md`,
`reports/profile/`, `python models.py` 가 단일 출처다.

---

## 0. 이번에 알아낸 것 — 계획이 바뀐 이유

| 사실 | 근거 |
| --- | --- |
| **GPU 클럭이 엔진을 바꾼다** | `unik3d` 같은 ONNX·같은 옵션·지문 바이트 동일인데 4번 빌드가 19.04 / 13.35 / 8.41 / 14.49 ms. 클럭 1800MHz 고정 후 3번 빌드가 9.66 / 9.62 / 9.68 (이 7번은 모두 opt_level 5. 기본값으로 되돌린 최종 빌드는 8.44) |
| **낡은 엔진이 2.3배 느릴 수 있다** | `distill_any_depth` 10.02 → **4.31 ms**. ONNX·빌드 설정 동일, 엔진·지문·타이밍 캐시를 지우고 재빌드한 것뿐 |
| **fp32 비중이 진단 지표다** | 같은 모델이 86.6% → **1.5%** 로 바뀌며 2.3배 빨라짐 |
| **인코더가 5.8배** | `metric3d_v2` ViT-L → ViT-S, 349.8 → **60.5 ms** |
| float64 는 전부 깨끗 | 12개 그래프 전수 0 — 이 항목 종결 |
| 어디가 느린지 볼 수 있게 됨 | `profile_model.py` (레이어별 시간 + 융합·정밀도 상태) |

**가장 큰 발견이 최적화가 아니라 결함이었다.** 낡은 엔진 때문에 2.3배가 그냥
버려지고 있었고, 벤치마크도 정확도 검증도 통과했기 때문에 **감지할 수단이 없었다.**

그리고 그 위에 더 근본적인 것이 있었다. TensorRT 는 빌드 중에 커널 후보를
**GPU에서 실제로 재보고** 고른다. 이 카드의 클럭은 210~2130MHz 를 오르내리므로
그 측정이 잡음이고, 커널 선택이 빌드마다 달라진다. `unik3d`(vits)처럼 fp16 과
fp32 커널이 그 잡음 폭 안에서 비슷한 모델은 매번 다른 엔진이 나온다.
**빌드가 재현되지 않으면 최적화 실험은 전부 동전 던지기다** — 실제로
"opt_level 5 가 2ms 빠르다"는 결론이 이 방식으로 한 번 나왔다가 무너졌다.

그래서 1순위가 "새 최적화"가 아니라 **"측정 조건을 고정하고 지금 숫자가 맞는지
확인"** 이다.

---

## 1. 1순위 — 지금 바로

| | 작업 | 이유 | 상태 |
| --- | --- | --- | --- |
| **0** | **클럭 고정 정책** — 빌드·측정 모두 `nvidia-smi -lgc 1800,1800` | 안 하면 커널 선택이 매번 다르다 | 적용 |
| **1** | **12개 지문·캐시 전부 삭제 후 클럭 고정 상태로 재빌드 → 연속 측정** | 지금 표의 숫자는 클럭이 흔들리던 조건에서 나왔다 | 완료 |
| **2** | **재발 방지** — 타이밍 캐시 이름에 ONNX 해시를 넣어 다른 그래프의 캐시가 재사용되지 않게 | 지문에 캐시가 안 들어가서 생긴 문제 | 완료 |
| **3** | **측정마다 클럭을 기록** (`clock_mhz` / `clock_max_mhz`) | 조건이 안 적힌 숫자는 재현할 수 없다 | 완료 |
| **4** | **fp32 비중을 측정마다 기록** | 낡은 엔진 결함을 자동으로 잡는 유일한 지표 | |
| **5** | **12개 전수 프로파일 재수집** (클럭 고정 후) | 흔들리던 조건의 프로파일을 판단 근거로 쓰지 않는다 | 완료 |

**1번 완료 후 수집한 숫자만 현재 결과로 사용한다.**

왜 1800MHz 인가: 이 3080 이 열 여유(54℃) 안에서 계속 유지하는 값. 최대
2130MHz 는 순간 부스트라 오래 못 버티고, 유휴 210MHz 는 실제 조건이 아니다.
값 자체보다 **모든 빌드와 측정이 같은 값을 쓴다는 것**이 중요하다.

빌드와 측정은 나눈다: 1차로 전부 빌드(1~2시간, 무인), 2차로 12개를 연속
측정(엔진이 있으므로 빠름). 같은 엔진도 세션이 다르면 약 2% 움직이므로,
12개를 12개 시점에 재면 그 흔들림이 모델 간 차이에 섞인다.

---

## 2. 2순위 — 모델별 최적화 (프로파일이 가리키는 것만)

| | 작업 | 기대 |
| --- | --- | --- |
| ~~4~~ | ~~`depth_anything_ac` 그래프 단순화 켜기~~ | **완료** — 10.09 → 4.36 ms |
| ~~5~~ | ~~`unik3d` fp32 원인 규명~~ | **완료 — 그래프가 아니라 클럭이었다.** 87.6% → **1.4%**, 15.90 → **8.44 ms** |
| ~~6~~ | ~~`moge_2` · `unidepth_v2` fp32 조사~~ | **완료 — 5번과 같은 원인.** moge_2 49.2% → **1.8%** (29.63 → **19.65**), unidepth_v2 56.6% → **0.5%** (9.76 → **8.25**) |
| 7 | uint8 입력 적용 (518²·388×518) | 모델당 0.6~0.8 ms. 측정 완료 |
| ~~7a~~ | ~~`moge_2` 내보내기 방식 A/B~~ | **완료 — 현재 Dynamo+단순화 유지.** 네 그래프 평균 차이는 최대 1.12%이고 Reformat+Slice는 전부 32.95~34.28%. `reports/tune/moge_ab/` 참조 |

**fp32 비중이 높던 3개가 한 원인으로 전부 닫혔다.** 그래프를 한 줄도 고치지
않았고, 빌드 조건만 고정했다. 클럭 고정 후 12개 전수 프로파일도 다시 수집했다.

기준: **속도·정확도·재현성 세 가지를 함께 보고한다.** 세션 간 흔들림이 약 2%
이므로 그보다 작은 차이는 이득이라 부르지 않는다.

---

## 3. 3순위 — 검증 (최적화의 안전장치)

| | 작업 | 이유 |
| --- | --- | --- |
| 8 | fp16 vs fp32 정확도 12개 | fp16 을 쓰는 근거가 아직 없다. 지금 검증은 엔진과 그 엔진을 만든 파일을 비교하므로 fp16 자체의 손해는 안 잡힌다 |
| 9 | **정답 데이터 (실내 1종)** | `metric3d_v2` 를 ViT-L → ViT-S 로 바꿔 5.8배를 얻었는데 **그 대가를 못 재고 있다** |
| 10 | 테스트 이미지 확대 | 지금 `data/example.jpg` 한 장. 종횡비가 다른 것 최소 1장 필요 |

**9번이 밀려 있는 것이 이 계획의 가장 큰 위험이다.** 인코더를 줄이는 것은
품질을 깎는 변경인데, 얼마나 깎였는지 잴 수단이 없다.

평가 기준은 **metric 우선, 없으면 relative.** 모델별 분류(업스트림 확인 완료):

| | 모델 |
| --- | --- |
| metric 8 | `depth_anything_v2`, `depth_anything_v3`, `depth_pro`, `metric3d_v2`, `metric_anything`, `moge_2`, `unidepth_v2`, `unik3d` |
| relative 2 | `depth_anything_ac`, `distill_any_depth` |
| 미확정 2 | `vggt`, `streamvggt` — 업스트림에 명시 없음. **정답 데이터가 생기면 판명된다** |

정렬 방식은 모델마다 `spec.json` 에 선언한다. 상대 깊이를 정렬 없이 평가하면
무조건 지고, 미터 모델을 정렬해 평가하면 미터를 맞히는 능력이 점수에서 사라진다.

---

## 4. 4순위

| | 작업 |
| --- | --- |
| 11 | 빌더 최적화 레벨 0~5 스윕 (12×6 = 72회, 무인). **클럭 고정 상태에서만** — 안 그러면 레벨 차이가 아니라 잡음을 재게 된다. `unik3d` 의 opt_level=5 도 이 이유로 되돌렸고, 이 스윕에서 다시 판정한다 |
| 12 | 전처리 일반화 — `core/preprocess.py` 가 완성돼 있는데 테스트만 쓴다. 시각화·데이터셋 평가의 전제 |
| 13 | 시각화 데모 — 포인트 클라우드 + 2D 옵션 (설계는 `REFACTOR_PLAN.md` §11) |

---

## 5. 보류 — 근거가 약하거나 비싼 것

| | 이유 |
| --- | --- |
| CUDA Graph | **상한 1.5% 측정됨.** `depth_anything_v2` 에서 전송·연산·회수를 뺀 나머지가 0.065 ms |
| BF16 | RTX 3080 에서 fp16 대비 이득이 있는지 불확실. 착수 전 확인 필요 |
| INT8 | 교정 데이터셋 + 정확도 기준 재수립 필요 |
| 해상도 축소 | **최적화가 아니다.** 다른 입력을 넣는 것 |
| 인코더 추가 축소 | 12개 전부 이미 업스트림이 제공하는 최소 |

---

## 6. 인코더 — 확정 (업스트림 클론에서 확인, 2026-08-13)

| 모델 | 사용 | 업스트림이 제공 |
| --- | --- | --- |
| metric3d_v2 | **vits** (변경됨) | vits / vitl / vitg2. vits 는 RAFT-4iter, vitl 은 8iter — 인코더만 바뀌는 게 아니다 |
| unidepth_v2 | **vits** (변경됨) | vitl14 / vitb14 / vits14 |
| unik3d | **vits** (변경됨) | vitl / vitb / vits |
| depth_anything_v3 | DA3Metric-Large | Metric 계열은 **Large 하나뿐**. metric 우선 + 최소 인코더의 교집합 |
| depth_anything_v2 / ac, distill, moge_2 | 이미 최소 | |
| depth_pro, metric_anything, vggt, streamvggt | 선택지 없음 | 단일 체크포인트 |

---

## 7. 오케스트레이션

**제약 (실측)**

- 기계 둘: 노트북(GPU 없음) / 데스크탑(RTX 3080, conda 12개)
- GPU 는 직렬 자원. 측정 중 빌드 금지
- **측정이 세션마다 약 2% 흔들린다.** 한 런 내부 표준편차는 0.12 ms
- 빌드가 길다: depth_pro 15분+, VGGT 5분
- ssh 가 끊겨도 원격 작업은 살아남지만 로그 파일이 잠겨 재실행이 막힌다

**원칙**

1. **빌드와 측정을 분리한다** — 전부 빌드 → 전부 한 번에 측정
2. 모델이 아니라 축으로 묶는다 (fp16/fp32 연구 = 12개 빌드 후 24개 일괄 측정)
3. GPU 없이 되는 일은 노트북에서 (전처리, spec, 시각화 코드, 문서, 테스트)
4. 엔진을 바꾸는 결정을 스윕(72회 빌드) 전에 끝낸다
5. 빌드 병렬 실행 안 함 (3080 10GB, depth_pro·VGGT 가 메모리를 크게 쓴다)

---

## 8. 지켜온 원칙

- 수치는 손으로 적지 않는다. 측정 → 파일 → 표 자동 생성
- 재현 조건을 함께 적는다 (입력 크기·정밀도·인코더·GPU·드라이버·버전)
- 검증 안 된 것을 완료로 표시하지 않는다
- 입력 크기가 다르면 같은 줄에 놓지 않는다
- 빌드 설정을 코드가 알아서 바꾸지 않는다
- **2% 보다 작은 차이는 이득이라 부르지 않는다**
- 못 한 것은 못 했다고, 이유를 모르면 모른다고 적는다
