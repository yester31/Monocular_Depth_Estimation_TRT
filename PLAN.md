# 작업 계획 — 정확도 기반 추론 최적화

갱신: 2026-08-14

대상 브랜치: `refactor/planning`

이 문서는 **앞으로 실행할 작업의 단일 계획서**다. 짧게 유지한다 — 다음에 무엇을
할지 보러 온 사람이 지난 결과를 다 읽어야 한다면 계획서로서 실패한 것이다.

| 문서 | 역할 |
| --- | --- |
| **이 문서** | 상태 · 다음 작업 · 실행 규칙 |
| [`docs/findings.md`](docs/findings.md) | **무엇을 재서 무엇을 알았나.** 단계별 결과와 근거 |
| [`docs/model_contracts.md`](docs/model_contracts.md) | 모델 14종의 계약, 전처리 유형, `tr2m` 평가 계약 |
| [`docs/history.md`](docs/history.md) | 끝난 리팩토링 계획, 디렉터리 이름 변경, 재현 불가능한 역사 측정 |
| [`docs/setup.md`](docs/setup.md) | 환경 구성과 알려진 함정 |
| [`tools/README.md`](tools/README.md) | 각 도구가 무엇이고 어떻게 쓰나 |

`docs/history.md` 의 오래된 "미완" 표시는 현재 작업 목록이 아니다. **현재 작업
목록은 이 문서 마지막 절 하나뿐이다.**

### 저장소 구조 — 2026-08-14 정리

루트에는 `run.py`(모델을 돌린다)와 `demo.py`(결과를 본다)만 둔다. 그 밖의 실행
가능한 것은 전부 `tools/`, 답이 나와 다시 돌릴 일이 없는 것은 `tools/retired/`.
지우지 않는다(규칙 9).

정리 전 루트에 파이썬 17개·계획 문서 4개가 있었고, 그중 절반은 이미 답이 나온
실험의 드라이버였다. **문서 7,096줄 대 코드 18,037줄** — 워커마다 보고서를 남기고
그것을 저장소에 넣은 결과였다. `reports/notes/` 4개는 내용을 옮긴 뒤 없앴다.

루트에는 `run.py` 와 `demo.py` 둘만 있다.

측정값의 단일 출처:

- 속도: `reports/bench/`, `reports/comparison.md`
- **정답 데이터 정확도: `reports/gt/`, `reports/gt.md`**
- **평가에 쓴 이미지 목록과 체크섬: `data/eval/*.json`**
- 엔진 대 ONNX 정확도: `reports/accuracy.json`, `reports/accuracy.md`
- 레이어 프로파일: `reports/profile/`
- 빌드 조건: 각 `<engine>.buildinfo.json`
- MoGe 내보내기 A/B: `reports/tune/moge_ab/`
- 모델 계약과 환경: 각 `models/<name>/spec.json`, `python tools/models.py`

상태 표기: `완료` · `진행 가능` · `결정 필요` · `보류` · `외부 제약`

---

## 1. 목표와 완료 정의

목표는 14개 모델을 같은 조건에서 비교하면서, 속도를 올려도 출력 의미와 품질을
잃지 않는 것이다. 전체 계획의 완료 조건은 다음 다섯 가지다.

1. 서로 다른 종횡비의 입력과 정답 깊이 데이터로 평가할 수 있다. — **정답 데이터 완료, 종횡비 확장 남음**
2. 각 모델의 출력 계약(metric/relative/geometry)에 맞는 지표를 사용한다. — **완료**
3. fp16과 fp32의 직접 비교 결과가 있고, 정밀도 선택 근거가 남아 있다. — **완료**
4. 채택한 최적화는 독립 빌드 3회, 정확도, 재현성 검증을 모두 통과한다.
5. 결과와 환경이 JSON에 기록되고 Markdown 표는 JSON에서 생성된다.

**정답 데이터 평가가 준비되기 전에는 인코더·해상도·INT8처럼 모델 품질을 바꿀
수 있는 변경을 채택하지 않는다.**

---

## 2. 현재 기준선

**모델 14개**, 전부 클럭 1800 MHz 고정에서 빌드·측정됨. 속도는
`reports/comparison.md`, 정답 데이터 정확도는 `reports/gt.md`.

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| GPU 클럭 1800 MHz 고정 정책 | 완료 | 빌드·측정 동일 조건. 빌드 전후 클럭을 `<engine>.buildinfo.json` 에 기록하고, 흔들렸으면 로드할 때마다 경고 |
| 14개 엔진 빌드·측정 | 완료 | `reports/comparison.md`. 표에 `fp32` · `moved` 열이 있어 엔진 상태가 속도 옆에 보인다 |
| 엔진 대 ONNX 정확도 | **완료 — 14/14** | `reports/accuracy.json`. FAIL 0, `depth_anything_v3` 하늘 마스크만 WARN (참조 평균 6.57e-06 이라 상대오차 8.79%가 부풀려짐, 상관 0.9984). `zipdepth` · `tr2m` 포함 전 모델 재실행 |
| 전수 레이어 프로파일 | **완료 — 14/14** | `reports/profile/`. `tr2m` 은 이미지와 기록된 `text_features [1,1,768]` 를 모두 바인딩해 재측정: 369/369 레이어, 19.338 ms, 이동 0.70%, fp32 출력 0.55% |
| **정답 데이터 평가 (DIODE 실내 50장)** | **완료 — 일반 11개 + scale-only 2개** | `reports/gt.md` · `reports/gt_scale_only.md`. `tr2m` 만 이미지별 프롬프트 계약이 없어 미측정 |
| **`unidepth_v2` · `unik3d` 672x896 재빌드** | **완료** | D11 의 3.1배 스케일 오차가 1.08 · 1.15 로 내려감. 모델 성질이 아니라 518 을 강제한 대가였다 |
| **`metric3d_v2` canonical → 미터** | **완료** | DIODE 가 전역 캘리브레이션을 제공(`fx=886.81`). "영구 차단" 으로 적혀 있었으나 임의 사진에 대해서만 참이었다 |
| **DIODE 깊이 규약** | **z-depth 로 측정 확인** | `tools/check_diode_convention.py`. 문서 어디에도 없어서 평면 피팅으로 판정 |
| **MoGe shift solver 재구현** | 완료 | `core/pointmap.py`. 원본과 최대 3.6e-05 일치(`tools/check_pointmap.py`). 평가가 모델 클론 없이 돈다 |
| 최소 인코더 적용 | 완료 | 제공 선택지가 있는 모델 전부 |
| MoGe 내보내기 방식 4종 A/B | 완료 | 12개 독립 빌드, 현재 Dynamo+단순화 유지 |
| `moge_2` · `metric_anything` 빌더 튜닝 | 완료 — 채택 없음 | 내보내기 4종 · 최적화 레벨 0~5 · 워크스페이스 2/4/8 GiB, 총 27빌드. 전부 무효 |
| `moge_2` 이동 비용 32% | 구조적, 종결 | `replicate` 패딩. ONNX 노드를 2282→633 으로 줄여도 불변 |
| `tr2m` · `zipdepth` 추가 | 완료 | 13번째·14번째 모델 |
| **공통 전처리 연결 (P1)** | **완료 — 13/14** | `core/preprocess.py`. `reports/inputs/*.npy` 와 바이트 동일. `depth_pro` 만 자기 경로에 남음 |
| **평가 입력 3종횡비 (P2)** | **완료** | `data/eval/aspects.json`. 4:3 · 16:9 · portrait, DIODE 에서 결정적 크롭 |
| **통합 시각화 데모 (P7)** | **완료 — headless + live** | `reports/demo/live/`. 3종횡비에서 실제 엔진 실행; tr2m 계약상 제외, metric3d_v2는 `--fx` 미지정 오류 패널, 나머지 12개 성공 |
| **`vggt`·`streamvggt` scale 정렬 채점** | **완료** | `reports/gt_scale_only.md`. 각 50장; 일반 미터 표와 분리 유지 |
| **테스트 240개가 실패 불가였던 문제** | **해결** | `tests/conftest.py`. 숨은 실패 3건 발견, 전부 테스트 버그 (`docs/findings.md`) |
| **발표 수치 전수 감사** | **완료** | 불일치 16 묶음 · 출처 없음 60 묶음. [`docs/findings.md`](docs/findings.md) 9절 |
| **미측정을 기계 판독 가능하게** | **완료** | `core/unmeasured.py`. 세 생성기가 빈칸을 `not measured` 로 표시하고 이유를 묶어 낸다 |

현재 기준선은 보존한다. 실험 ONNX 와 엔진은 별도 이름·별도 결과 경로를 쓰며,
채택 결정 전에는 `reports/bench` 나 발표용 엔진을 교체하지 않는다.

---

## 3. 단계별 상태

결과와 근거는 전부 `docs/findings.md` 의 같은 이름 절에 있다.

| 단계 | 상태 | 남은 것 |
| --- | --- | --- |
| P0 측정 메타데이터 | **완료** | — |
| P1 공통 전처리 연결 | **완료 13/14** | `depth_pro` 는 자기 경로에 남음(중단 조건). `models/*/infer.py` 미전환 |
| P2 평가 입력 세트 | **완료** | — |
| P3 정답 데이터 평가 | **일반 11 + scale-only 2 완료** | `tr2m` 이미지별 프롬프트·embedding 계약만 남음 |
| P4 fp16 대 fp32 | **완료 8측정 / 2빌드불가** | — |
| P5 보류 변경 결정 | **D2·D3 완료 / D4 대기** | D4 는 사용자 결정 |
| P6 빌더 레벨 스윕 | **완료** | `streamvggt` 레벨 4 발표 엔진 교체·정확도 확인 완료 |
| P7 시각화 데모 | **완료 — headless + live** | 3종횡비 실제 엔진 산출물은 `reports/demo/live/` |

**P6 이 닫혔다.** `streamvggt` 레벨 4 가 확인 6빌드를 통과했다 — 레벨 3 세 빌드가
53.30/53.43/54.03, 레벨 4 가 52.10/52.28/52.69 로 **분포가 겹치지 않는다**(−2.3%).
**14개 중 이 하나만 레벨을 지정한다.**

여기서 얻은 더 중요한 숫자는 레벨 3 자신의 폭 0.73 ms(1.4%)다. 클럭을 고정했는데도
같은 설정이 그만큼 흔들린다.

---

## 4. D4 — Phase 6 범위 · **사용자 결정 대기**

다음 단계는 자동으로 시작하지 않는다. 별도 승인 후 범위를 하나씩 연다.

- CPU 추론 백엔드
- BF16
- INT8
- CUDA Graph

---

## 5. 보류·외부 제약

| 항목 | 상태 | 재개 조건 |
| --- | --- | --- |
| MoGe 메시 출력 | 보류 | 고정 `utils3d`에 맞춘 포팅 + 실제 GLB 시각 검증 |
| VGGT split 엔진 | 외부 제약 | 현재 업스트림 export 문제가 해결되거나 호환 패치 검증 |
| BF16 | 보류 | P3/P4 완료 후 fp16 대비 목적과 기준 합의 |
| INT8 | 보류 | 교정 데이터와 정확도 허용치 합의 |
| CUDA Graph | 보류 | 측정 상한 약 1.5%보다 우선순위 높은 작업 완료 |
| 해상도 축소 | 보류 | 최적화가 아니라 품질 변경이므로 P3 평가 후 별도 실험 |
| 추가 인코더 축소 | 종료 | 14개 모두 업스트림 제공 최소 또는 선택지 없음. 선택지는 README의 인코더 표에 기록 |

---

## 6. 실행 규칙

0. **GPU 클럭을 고정하지 않고 빌드하지 않는다** (`nvidia-smi -lgc 1800,1800`,
   해제는 `-rgc`). TensorRT 는 빌드 중 GPU 에서 커널 후보를 실제로 재보고
   고르므로, 클럭이 흔들리면 커널 선택이 부분적으로 난수가 된다. 같은 ONNX·같은
   옵션·지문 바이트 동일인 unik3d 빌드 4회가 19.04 / 13.35 / 8.41 / 14.49 ms 였고,
   클럭 고정 후 3회가 9.66 / 9.62 / 9.68 이었다. 엔진마다
   `<engine>.buildinfo.json` 에 빌드 전후 클럭이 기록되고, 두 값이 다르면
   `common.get_engine()` 이 로드할 때마다 경고한다. **경고가 뜨는 엔진의 속도는
   그 모델의 속도가 아니다.**
1. GPU 빌드와 측정은 병렬 실행하지 않는다. **그리고 시작 전에 카드가 비어 있는지
   확인한다** (`nvidia-smi`). 원격 명령이 잘려도 그쪽 프로세스는 죽지 않으므로,
   잘린 작업이 다음 측정을 오염시킬 수 있다. 긴 원격 GPU 작업은 `schtasks` 로
   띄워서 `schtasks /end` 로 끝낼 수 있게 한다.
2. 원격 작업은 실제 프로세스·로그·결과 파일을 확인하기 전 “실행 중” 또는 “완료”로
   보고하지 않는다. SSH 세션에서 분리한 프로세스가 살아남는다고 가정하지 않는다.
3. 원격 코드를 갱신하기 전에 측정 결과를 먼저 회수한다.
4. 기존 엔진을 덮어쓰지 않고 variant별 파일명을 사용한다.
5. 한 번의 빌드나 2% 미만 차이를 이득으로 발표하지 않는다.
6. 속도·정확도·재현성 중 하나라도 없으면 채택하지 않는다.
7. 수치는 손으로 표에 옮기지 않고 JSON에서 생성한다.
8. 입력 크기나 출력 의미가 다른 모델을 같은 순위로 오해하게 만들지 않는다.
9. 실패·OOM·unsupported를 결과에서 삭제하지 않는다.
10. 변경 후 최소 검증:
    - `python -m py_compile <changed python files>`
    - `git diff --check`
    - 관련 단위 테스트
    - 전체 `tests/` 회귀 테스트 — **종료 코드를 확인한다.** 파이프로 넘기면
      실패한 실행을 놓친다(실제로 놓친 적이 있다).
    - GPU 변경이면 엔진 정확도와 반복 측정
11. **테스트를 새로 쓰면 그 테스트가 실패할 수 있는지 먼저 확인한다.** 일부러
    깨뜨려 보고 실제로 빨간불이 뜨는지 본다. 이 저장소의 테스트 240개가 그 확인을
    받지 않아 실패할 수 없는 상태로 오래 있었다
    (`docs/findings.md` "검증 자체를 검증한 결과"). "통과했다" 는 그 테스트가
    실패할 수 있을 때만 정보다.
12. **회귀 테스트가 무엇을 무엇과 비교하는지 적는다.** `gt.md` 회귀 테스트는
    생성기를 파일과 비교했지 파일을 원본 JSON 과 비교하지 않아서, 표에 열을 추가해도
    통과했다 — 일관성 테스트를 내용 테스트로 착각한 것이다. 음성 대조군(일부러
    틀린 입력)으로 판별력을 확인한다.

---

## 7. 최근 완료 묶음과 남은 작업 — 2026-08-14 기준

### 데스크탑(RTX 3080) 묶음 — 완료

1. `streamvggt` 레벨 4 발표 엔진 교체와 ONNX 정확도 확인 완료. 새 벤치는
   52.827 ms, 엔진 대 ONNX 상대오차 0.1063%, 상관 0.999983.
2. 엔진 대 ONNX 정확도 전수 재실행 완료: **14/14**, FAIL 0.
3. `tr2m` 프로파일 완료. 첫 배치는 두 번째 입력을 채우지 않은 무효 측정이었고,
   `profile_model.py` 를 고친 뒤 기록된 CLIP embedding까지 바인딩해 재측정했다.
4. DIODE 전체 GT 재채점 완료. 첫 배치는 데이터 루트를 잘못 잡아 0장을 쓰고도
   종료 코드 0이었으며, `--root C:\Users\soy\data\diode\val` 로 재실행해 일반
   11개와 scale-only 2개가 각각 50장을 처리했음을 JSON에서 확인했다.
5. `demo.py --live` 완료. 3종횡비 실제 산출물과 summary는 `reports/demo/live/`.

### 노트북에서 완료한 것

6. **완료 — `spec.json` 의 `verified: "byte-exact"` 를 사실에 맞게 고쳤다.**
   전 모델 `check_adapter` 로그를 다시 확인했다(2026-08-14): 어댑터가 있는
   13개 중 정확히 같은 것은 10개이고 `depth_anything_ac`(2.384e-07) ·
   `unidepth_v2`(4.768e-07) · `unik3d`(4.768e-07) 는 `< 1e-4` 근사다.
   `adapter_check` 에 oracle·실측 최대 차이·허용치를 구조화했고,
   `tests/test_preprocess_spec.py` 는 이제 설명 문구가 아니라 이 필드로 어댑터
   존재와 판정 근거를 검사한다.
7. `models/*/infer.py` 의 전처리는 공통 코드로 옮기지 않았다(`docs/findings.md` P1).
   대조할 기록된 tensor가 없는 상태에서는 안전하게 검증할 수 없어 기존 중단 조건을 유지한다.

### 남은 것

8. **완료 — `streamvggt` 재프로파일.** `stale` 이 사라졌다. 레이어 수가 714 로
   그대로이고 fp32·이동 비중도 사실상 같다 — 레벨 4 는 그래프가 아니라 커널을
   바꾼다(`docs/findings.md` P6).
9. **레벨 4 의 폭을 기록에 반영했다**(`docs/findings.md` P6). 발표 대 발표 이득은
   −0.96% 이고 확인 실험의 −2.3% 가 아니다. 추가 조치는 필요 없지만, 다음에 레벨을
   채택할 때 **3빌드가 분포를 다 보여주지 못한다**는 것을 전제로 한다.

### 사용자 결정이 필요한 것

8. **D4 — Phase 6 범위.** 보류 모델 16개는 정적 검토 후 소스 백업을 삭제했고,
   제외 근거만 [`docs/later_candidates.md`](docs/later_candidates.md)에 남겼다.
   남은 선택지는 CPU 백엔드, BF16, INT8, CUDA Graph다. §4와 §5에 따라 하나씩
   열며 임의로 시작하지 않는다.
