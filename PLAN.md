# 앞으로의 작업 계획

2026-08-13 작성. 리팩토링 완료 시점(`REFACTOR_PLAN.md` §10) 이후의 계획.

| 문서 | 역할 |
| --- | --- |
| `REFACTOR_PLAN.md` | **끝난 리팩토링의 기록.** 구조 정리·버그 수정·측정 통일이 어떻게 됐는지 |
| **이 문서** | **앞으로 할 일.** 무엇을, 왜, 어떻게, 어떻게 검증할지 |
| `docs/model_contracts.md` | 모델 12종의 사실 관계와 측정 결과 |

---

## 1. 이 저장소로 하려는 것

원래 두 가지였다.

1. **각 모델을 최대한 빠르게 만든다** (모델별 추론 최적화)
2. **모델들을 공정하게 비교한다**

여기에 두 가지가 추가됐다.

3. **결과를 눈으로 확인한다** — 포인트 클라우드 포함, 히트맵 이상의 여러 방식
4. **지금 쓰는 최적화 방식이 최선인지 검토한다** — 더 나은 방법을 찾고 평가한다

---

## 2. 지금 어디까지 왔나

| 목표 | 상태 | 한 줄 요약 |
| --- | --- | --- |
| 2. 비교 | **거의 됨** | 12개가 같은 방식으로 측정되고, 표가 자동 생성된다 |
| 1. 최적화 | **거의 안 됨** | 12개 전부 TensorRT 기본 설정 + 16비트. 튜닝한 적 없다 |
| 3. 시각화 | **최소한만** | 히트맵 1장씩. 포인트 클라우드는 3개 모델만 |
| 4. 최적화 검토 | **안 함** | 이 문서가 그 시작 |

**최적화를 지금까지 안 한 것은 순서 때문이다.** 최적화를 하려면 "빨라졌는지 잴 자"와
"결과가 안 망가졌는지 볼 눈"이 먼저 있어야 하는데, 시작 시점엔 둘 다 없었다.
그 둘을 만드는 것이 리팩토링이었고, 이제 둘 다 있다.

---

## 3. 작업 묶음 네 개

| | 묶음 | 핵심 |
| --- | --- | --- |
| **A** | 리팩토링 마무리 | 전처리 코드 일반화 — 만들어놓고 안 쓰는 모듈이 있다 |
| **B** | 추론 최적화 | 공정성 문제 → 확실한 것 → 훑어야 아는 것 → 위험한 것 |
| **C** | 검증 강화 | 테스트 이미지가 한 장뿐이다 |
| **D** | 시각화 데모 | 포인트 클라우드 + 2D 여러 방식 |

---

## A. 리팩토링 마무리

### A1. 전처리 일반화 — **가장 큰 남은 항목**

**무엇이 문제인가**

`core/preprocess.py` 는 완성되어 있다. 리사이즈 6종, 정규화 5종을 전부 구현하고,
모델별 차이는 데이터(어느 종류·어떤 값)로 선언하게 되어 있다.

**그런데 12개 스크립트 중 아무도 쓰지 않는다.** 테스트만 쓴다. 각 스크립트는
여전히 자기 전처리를 직접 구현하고 있다 — 함수로 뽑힌 것만 174줄이고,
5개 모델은 함수로 뽑지도 않고 `main()` 안에 인라인으로 들어 있다.

이건 "일반화를 했는데 적용을 안 한" 상태다. **중복은 그대로 남아 있고,
모듈만 하나 더 늘었다.**

**어떻게 하는가**

모델마다 이 순서로 한다.

1. 현재 전처리 코드를 읽고 어떤 종류·어떤 값인지 파악한다
2. 그것을 `spec.json` 에 데이터로 적는다
3. `core/preprocess.py` 로 같은 입력을 만들어본다
4. **`reports/inputs/<모델>.npy` 와 바이트 단위로 같은지 확인한다**
5. 같으면 스크립트의 전처리 코드를 호출 한 줄로 교체한다

**검증 방법이 이미 저장소에 있다** — 4번이 핵심이다.
`reports/inputs/*.npy` 12개는 **엔진에 실제로 들어간 바로 그 텐서**다.
그래서 재작성이 맞는지 GPU 없이, 추측 없이, 바이트 단위로 확인할 수 있다.
**같지 않으면 틀린 것이다.** 애매한 판단이 들어갈 여지가 없다.

**모델별 예외는 어떻게 되는가**

말씀하신 "일반화할 건 일반화, 예외는 예외"가 정확히 이 형태가 된다.

| | 일반화 (`core/preprocess.py`) | 예외 (`spec.json`) |
| --- | --- | --- |
| 리사이즈 | 6종 구현 | 어느 종류를 쓰는가 |
| 배수 맞추기 | 올림/내림/반올림/제약 4종 | 어느 규칙인가 (같은 4:3 사진이 700 이 되기도 686 이 되기도 한다) |
| 정규화 | 5종 구현 | 어느 종류, 어떤 평균·표준편차 |
| 여백 채우기 | 구현 | 검정인가 흰색인가 (모델마다 다르다) |
| 리사이즈 시점 | uint8 먼저 / float 먼저 | 어느 쪽인가 (보간 넘침 처리가 달라 결과가 바뀐다) |
| 좌표 되돌리기 | `Geometry` 로 구현 | — |

**규모**: 12개 모델. 모델당 30분~1시간. GPU 불필요.

**부수 효과**: 이게 끝나면 **아무 이미지나 아무 모델에 넣을 수 있게 된다.**
지금은 각 스크립트가 `example.jpg` 를 하드코딩해서 읽는다. C(테스트 이미지 확대)와
D(시각화 데모)가 전부 여기에 의존한다.

### A2. 그래프 단순화 기준 통일

**무엇이 문제인가**

ONNX 그래프 단순화(불필요한 연산 노드 정리)가 모델마다 제각각이다.

| 켜짐 8개 | 꺼짐 3개 | 설정 자체가 없음 1개 |
| --- | --- | --- |
| | `depth_anything_ac`, `streamvggt`, `vggt` | `depth_pro` |

**왜 껐는지 아무 데도 적혀 있지 않다.** 단순화가 그래프를 망가뜨려서 껐을 수도
있고, 그냥 안 해본 것일 수도 있다. **지금은 구분이 안 된다.**

**어떻게 하는가**

3개를 켜서 export 해보고, 되면 켜고 안 되면 **왜 안 되는지 주석으로 남긴다.**
결과가 바뀌는지는 정확도 검증으로 확인한다.

**규모**: 4개 모델 export. 데스크탑 필요.

### A3. VGGT 3분할 경로 포팅

업스트림이 바뀌어 지금 코드로는 export 가 안 된다. 원인은 규명·기록되어 있다
(`docs/model_contracts.md` D19). 엔진 세 개가 주고받는 데이터 형식을 다시
정의해야 하므로 한 줄 수정으로는 안 된다.

**보류로 두는 것이 맞다.** 단일 엔진 경로는 정상 동작하고, 3분할이 더 빠르다는
근거도 아직 없다.

---

## B. 추론 최적화 — 검토와 실행

### B0. 지금 방식 평가

**현재**: 12개 엔진 전부 "TensorRT 기본 설정 + 16비트". 모델별로 최적을 찾은 것이 아니다.

| 튜닝 축 | 현재 | 탐색 여부 |
| --- | --- | --- |
| 빌더 최적화 레벨 (0~5) | 전부 기본값 3 | **없음** |
| 숫자 정밀도 | 11개 16비트, 1개 32비트 | **근거 없음** |
| 작업 메모리 | 2개만 늘림 | 튜닝 아님 — 빌드 실패 때문 |
| 8비트 / CUDA Graph / 다중 스트림 | 없음 | 없음 |

**지금까지 실제로 빨라진 것은 한 건뿐이다.** `unidepth_v2` 가 20% 빨라졌는데
(64비트 실수가 섞여 연산 융합을 막고 있었다), 그것도 속도를 찾다가 아니라
정확도를 검증하려다 걸렸다.

**즉 "지금 방식이 최선인가"의 답은 명확하다 — 지금은 방식이라고 할 것이 없다.**

### B1. 공정성 문제 — 최적화보다 먼저

비교표를 보기 전에 고쳐야 할 것이 두 개 있다. **둘 다 성능 차이의 원인을
모델이 아닌 다른 곳에 두고 있다.**

**(1) 인코더 크기가 제각각이다**

| 크기 | 모델 |
| --- | --- |
| Small / ViT-S | `depth_anything_v2`, `depth_anything_ac`, `moge_2`, `distill_any_depth` |
| ViT-B | `unidepth_v2`, `unik3d` |
| **ViT-L** | **`metric3d_v2`**, `depth_anything_v3`(LARGE) |

`metric3d_v2` 가 가장 느린 이유의 상당 부분이 **"유일하게 제일 큰 인코더를
쓰기 때문"**일 수 있다. 그건 모델의 효율이 아니라 우리가 고른 설정이다.

→ 같은 급으로 맞출 수 있는 모델은 맞추거나, **맞출 수 없다면 표에 인코더 크기를
같이 적어야 한다.** 지금 표에는 그 정보가 없다.

**(2) `metric3d_v2` 만 32비트다**

```python
precision = "fp32"   # 'fp32'
```

주석이 선택지를 32비트 하나로 적어놨는데 **이유가 없다.** 그리고 이 모델이
가장 느리다(349밀리초). 16비트가 되면 대략 절반이 될 수 있다.

→ **16비트로 시도한다.** 되면 큰 이득이고, 안 되면 왜 안 되는지 기록한다.
지금은 그 기록조차 없다.

### B2. 확실한 것 — 바로 한다

| | 작업 | 기대 | 방법 |
| --- | --- | --- | --- |
| 1 | `metric3d_v2` 16비트 | 349 → 약 180밀리초 | 설정 한 줄 + 빌드 + 정확도 확인 |
| 2 | 그래프 단순화 통일 (A2) | 미상, 위험 없음 | export 다시 |
| 3 | 64비트 실수 잔존 검사 | `unidepth_v2` 에서 20% 나왔던 것 | 도구가 이미 있다 (`core/onnx_tools.count_float64`) — 12개 전수 |
| 4 | uint8 입력 (`depth_anything_v2`) | −20.7%, 이미 측정됨 | 적용만 하면 된다 |

3번은 특히 해볼 만하다. **한 모델에서 20%가 나왔는데 나머지 11개는 검사한 적이 없다.**

### B3. 훑어야 아는 것

**빌더 최적화 레벨 0~5 스윕**

TensorRT 가 엔진을 만들 때 "얼마나 열심히 찾을지"를 정하는 값이다.
높이면 빌드는 오래 걸리고 추론은 빨라질 수 있다. **결과는 안 바뀐다.**

- 12개 × 6단계 = 72번 빌드. 사람 손은 안 가고 시간만 든다
- `get_engine` 이 이미 이 값을 인자로 받고, 엔진 지문에도 포함한다 — **스윕 스크립트만 있으면 된다**
- 위험 없음: 결과가 안 바뀌므로 정확도 재검증도 불필요

**출력 자료형 줄이기**

`depth_pro` 는 결과를 GPU→CPU 로 옮기는 데 2.9밀리초를 쓴다. 32비트 대신
16비트로 내보내면 절반이 된다. 전체의 1% 남짓이라 크진 않지만 공짜에 가깝다.

### B4. 위험하거나 비싼 것 — 나중에

| | 왜 나중인가 |
| --- | --- |
| **8비트(INT8)** | 속도는 1.5~2배 기대되지만 **교정용 데이터셋**이 따로 필요하고, 정확도 기준을 다시 세워야 한다. 지금 기준(0.03~0.92%)은 확실히 깨진다 |
| **BF16** | RTX 3080 에서 16비트 대비 이득이 있는지 **불확실하다.** 소비자용 칩은 데이터센터 칩과 텐서코어 구성이 다르다. **착수 전에 이것부터 확인해야 한다** |
| **CUDA Graph** | 근거가 약하다 — 아래 |

**CUDA Graph 의 상한은 이미 측정됐다**

구간 계측기로 재보면, `depth_anything_v2` 에서 전송·연산·회수를 뺀 나머지
(호출 오버헤드와 동기화)는 **0.065밀리초, 전체의 1.5%** 다. CUDA Graph 가
노리는 것이 그 부분이다.

단, GPU 안에서 커널 사이의 빈 시간은 "연산" 쪽에 포함되어 이 숫자에 안 잡힌다.
그것까지 줄일 여지는 남아 있다. **그래도 기대했던 것보다 근거가 약하다.**

### B5. 최적화 결과를 어떻게 평가하는가

**모든 최적화는 이 세 가지를 함께 보고한다.**

1. **속도** — 구간별로(전송/연산/회수). 총합만으로는 원인을 알 수 없다
2. **정확도** — 바꾸기 전 엔진과 비교. 상대오차와 상관관계 둘 다
3. **재현성** — 같은 엔진도 세션이 다르면 **약 2% 움직인다.** 그보다 작은 차이는 이득이라 부르지 않는다

3번은 실측으로 확인된 사실이다. 같은 엔진을 다시 재서 4.23 → 4.31밀리초가
나왔다. 한 번의 측정 안에서 계산한 표준편차는 0.12밀리초뿐이라, **레코드의
표준편차는 "다시 돌렸을 때의 흔들림"을 말해주지 않는다.**

---

## C. 검증 강화

### C1. 테스트 이미지 확대 — **가장 싸고 가장 크게 개선된다**

```
data/
  example.jpg      ← 지금 이것뿐
```

**12개 모델 전부 이 사진 한 장으로만 검증됐다.** 정확도도, 시각화도, 성능도
전부 이 한 장이다. 한 장에서 통과한 것이 일반적으로 통과한다는 뜻이 아니다.

필요한 것: 실내/실외, 밝은/어두운, 가까운/먼, 반사·유리, 하늘 포함 등 **5~10장.**

**A1(전처리 일반화)이 선행되어야 한다.** 지금은 스크립트가 파일 이름을
하드코딩하고 있어서 다른 사진을 넣으려면 12곳을 고쳐야 한다.

### C2. 16비트 손실 측정 — 한 번도 안 했다

지금 정확도 검증은 "**엔진 결과 vs 그 엔진을 만든 파일의 결과**"다.
**둘 다 16비트라서 16비트 자체의 손해는 안 잡힌다.**

11개 모델이 16비트로 도는데 **그 근거가 "다들 그렇게 하니까"뿐이다.**

→ 모델마다 32비트 엔진을 하나씩 더 만들어 비교한다. 그러면 "이 모델은 16비트로
손해가 거의 없다 / 이 모델은 크다"가 나오고, **그게 정밀도를 고를 근거가 된다.**
B1의 `metric3d_v2` 문제도 여기서 함께 답이 나온다.

### C3. PyTorch 원본과의 비교

"엔진이 원래 모델과 같은 답을 내는가"는 아직 모른다. 일부러 분리해 뒀다 —
섞으면 전처리 차이인지 변환 차이인지 구분이 안 되기 때문이다.
**A1이 끝나면 전처리가 한 곳으로 모이므로, 그때 비로소 이 비교가 깨끗해진다.**

### C4. 정답 데이터(GT) 확보 — 검토

**무엇이 빠져 있는가**

지금 "정확도"는 **변환 손실만** 잰다. 엔진이 원본 파일과 같은 답을 내는지는
확인했지만, **그 답이 맞는 답인지는 확인한 적이 없다.**

`core/golden.py` 에는 이 분야의 표준 지표(AbsRel, RMSE, δ1)가 이미 구현되어
있다. **비교할 정답이 없어서 못 쓰고 있을 뿐이다.**

**왜 이 저장소에 특히 중요한가**

속도만 비교하면 **"빠르지만 틀린 모델"이 이긴다.** 지금 표가 정확히 그 위험에
노출되어 있다. 그리고 이미 문서화된 문제 — `unidepth_v2`·`unik3d` 의 미터
스케일이 3.15배 어긋난다 — 도 **정답이 있으면 숫자로 확정된다.** 지금은
"업스트림 대비"로만 재서 실제로 얼마나 틀렸는지는 모른다.

**깊이 정답만으로는 부족하다**

12개가 서로 다른 것을 내놓기 때문에 필요한 정답도 다르다.

| 모델이 내는 것 | 필요한 정답 | 해당 모델 |
| --- | --- | --- |
| 상대 깊이 | 깊이 (스케일·시프트 맞춘 뒤 평가) | `depth_anything_v2/ac/v3`, `distill_any_depth` |
| 미터 깊이 | 깊이 (맞추지 않고 그대로) | `depth_pro`, `depth_anything_v2`(metric) |
| 점 지도 | 깊이 **+ 카메라 내부 파라미터** | `unidepth_v2`, `unik3d`, `moge_2`, `metric_anything` |
| 초점거리·내부 파라미터 | **카메라 캘리브레이션** | `depth_pro`, `unidepth_v2` |
| 카메라 자세 | **포즈** | VGGT 3분할 경로 |
| canonical 깊이 | 깊이 + **실제 초점거리** | `metric3d_v2` |

**즉 깊이만 있는 데이터보다 카메라 정보가 함께 있는 데이터가 훨씬 가치 있다.**
이 저장소 모델의 절반이 카메라를 추정하기 때문이다.

**후보 데이터** — 아래는 **기억에 근거한 것이며 검증되지 않았다.**
실제 규격·라이선스·배포 형태는 내려받기 전에 확인해야 한다.

| 데이터 | 성격 | 이 저장소에 맞는 점 | 주의 |
| --- | --- | --- | --- |
| **NYU Depth V2** (테스트 654장) | 실내, 미터, 내부 파라미터 고정 | 가장 널리 보고되어 **우리 숫자를 논문 값과 대조**할 수 있다 | Kinect 특성상 경계·반사면이 부정확 |
| **KITTI** (Eigen 테스트 697장) | 실외 주행, LiDAR, 미터, 캘리브레이션 제공 | 실외를 커버 | 정답이 희소 → **유효 마스크 필수** |
| **iBims-1** | 실내 소규모, 고품질 | 경계·평면성 평가에 설계됨 | 장면 수가 적다 |
| **DIODE** | 실내+실외, 레이저 | 한 데이터로 양쪽 | 용량이 크다 |
| **ETH3D / Middlebury** | 고해상도, 레이저·스테레오 | 정밀도가 높다 | 장면 수가 적다 |
| **Virtual KITTI 2 / Replica** (합성) | 완벽한 깊이 + 포즈 + 내부 파라미터 | **점 지도·카메라까지 평가 가능** | 실사와 도메인 차이 |

**피해야 할 것**: **Hypersim.** `depth_anything_v2` 의 기본 체크포인트가
Hypersim 으로 학습되어 있다. 그 모델에만 유리한 평가가 된다.

**권장 단계**

| | 데이터 | 목적 |
| --- | --- | --- |
| 1단계 | **NYU 테스트 세트** | 실내 미터 + 내부 파라미터. 가장 표준적이고 규모가 감당된다 |
| 2단계 | **KITTI Eigen 테스트** | 실외 |
| 3단계 | 합성 1종 (포즈·내부 파라미터 정답 포함) | 점 지도·카메라 평가 |
| 상시 | 직접 찍은 사진 5~10장 | 정답 없이 **눈으로** 보는 용도 (C1) |

**어떻게 하는가**

1. **데이터는 저장소에 넣지 않는다.** `datasets/` 를 무시 목록에 넣고,
   **내려받기 스크립트와 체크섬 명세만** 커밋한다
2. **평가 규칙을 모델마다 선언한다** (`spec.json`) — 정렬 방식(없음 / 스케일만 /
   스케일+시프트), 유효 마스크 규칙, 미터 변환 필요 여부.
   **이걸 코드에 흩어놓으면 지금 전처리가 겪은 문제를 그대로 반복한다**
3. 지표 계산은 이미 있다 (`core/golden.py`)
4. 결과는 JSON 으로 저장하고 표는 자동 생성 — 속도 표와 같은 방식

**정렬 방식이 왜 선언 대상인가**: 상대 깊이 모델을 정렬 없이 평가하면 무조건
지고, 미터 모델을 정렬해서 평가하면 **미터를 맞히는 능력이 점수에서 사라진다.**
어느 쪽을 쓰느냐가 결과를 뒤집으므로 모델마다 못 박아야 한다.

**규모**: 데이터 확보 반나절, 평가 배선 1~2일, 12개 실행 반나절.

**선행 조건**: A1(전처리 일반화). 데이터셋 이미지를 12개 모델에 넣으려면
전처리가 한 곳으로 모여 있어야 한다.

---

## D. 시각화 데모

`REFACTOR_PLAN.md` §11 에 설계를 적어뒀다. 요지만 옮기면:

- **포인트 클라우드**: 모델이 3부류로 나뉜다 — 점 좌표를 직접 내는 4개,
  자체 카메라 정보로 역투영 가능한 1개, 초점거리를 가정해야 하는 7개.
  일반화되는 것은 역투영·색·마스크·저장·뷰어이고, 예외는 "점을 어디서 얻는가" 하나다
- **2D 보기**: 히트맵 하나로는 부족하다. 실제 값 눈금, 단면 그래프,
  분포 히스토그램, 지점 조회, 모델 간 차이 지도, 12개 격자 비교 — **옵션으로 고른다**
- 히트맵은 이미지마다 최소~최대를 다시 정규화하므로 **미터 정보가 화면에서 사라진다.**
  미터를 내는 모델과 상대 깊이만 내는 모델이 똑같아 보인다

**시각화는 최적화의 안전장치이기도 하다.** 정밀도나 설정을 바꾸면 결과가 미세하게
달라지는데, "이 정도 차이가 괜찮은가"는 숫자만으로 판단할 수 없다.

---

## 4. 작업 오케스트레이션

작업이 많아졌으므로 "무엇을"만큼 "어떤 순서로, 어디서, 어떻게 묶어서"가 중요해졌다.
아래는 이번 작업에서 **실제로 부딪혀 확인한 제약**에서 나온 것이다.

### 4.1 제약 조건 (실측)

| 제약 | 내용 |
| --- | --- |
| 기계가 둘 | 노트북(GPU 없음, numpy·cv2 있음) / 데스크탑(RTX 3080, conda 환경 12개) |
| **GPU 는 직렬 자원** | 빌드도 측정도 한 번에 하나 |
| **측정이 세션마다 ~2% 흔들린다** | 같은 엔진을 다시 재서 4.23 → 4.31밀리초. 한 런 내부 표준편차는 0.12밀리초뿐 |
| 빌드가 길다 | VGGT 5분, depth_pro 15분 이상 |
| ssh 가 끊겨도 원격 작업은 살아남는다 | 다만 로그 파일이 잠긴 채 남아 **재실행이 막힌다** |
| 결과 파일은 데스크탑이 쓰고 노트북이 커밋한다 | 회수 전에 git 조작 금지 — `tools/sync_desktop.sh` 가 강제 |

### 4.2 원칙 여섯 개

**1. 빌드와 측정을 분리한다** — 가장 중요하다

빌드는 **무인·야간·순서 무관**이다. 측정은 **유휴 GPU 에서 연속 한 번에** 해야 한다.

이유가 위의 2% 흔들림이다. 모델마다 "빌드하고 바로 재고 다음 모델로" 넘어가면
12개 숫자가 12개 다른 시점에 측정되고, **그 흔들림이 모델 간 차이에 섞여 들어간다.**
지금 표의 몇몇 근접한 행들은 이미 그 영향 안에 있을 수 있다.

→ **전부 빌드 → 전부 한 번에 측정.**

**2. 모델이 아니라 축으로 묶는다**

예: 16비트 vs 32비트 연구는 이렇게 한다.

```
잘못:  모델1 32비트 빌드→측정, 모델2 32비트 빌드→측정, ...
맞음:  32비트 12개 전부 빌드 (몇 시간, 무인)
       → 24개(16비트 12 + 32비트 12)를 한 번에 연속 측정
```

**3. 일은 있어야 할 곳에서 한다**

| 노트북 (GPU 불필요) | 데스크탑 (GPU 필요) |
| --- | --- |
| 전처리 일반화 + 바이트 대조 | export |
| spec 편집·검증 | 엔진 빌드 |
| 시각화 코드 | 측정 |
| 문서·테스트 | 정확도 검증 |

이 경계는 이미 테스트가 강제하고 있다 — `core/spec.py` 와 `core/artifact.py` 는
torch·TensorRT 를 import 하지 않으며, 테스트가 그것을 검사한다.

**4. GPU 없이 확인 가능한 것은 전부 GPU 없이 한다**

전처리 바이트 대조, spec 검증, 미정의 이름 검사, 콘솔 인코딩, export 플래그 —
전부 노트북에서 된다. **GPU 는 GPU 만 할 수 있는 일에 쓴다.**

**5. 측정 중에는 GPU 를 독점한다**

빌드가 도는 중에 벤치마크를 돌리면 그 숫자는 버려야 한다. 잠금 파일 하나로
막을 수 있다.

**6. 긴 작업은 끊겨도 살아남게 만든다**

이번에 ssh 타임아웃으로 로그가 잠기고 재실행이 막히는 일이 있었다.

### 4.3 엔진을 바꾸는 결정을 먼저 한다

순서를 잘못 잡으면 **통째로 재작업**이 된다.

| 결정 | 영향 |
| --- | --- |
| 인코더 크기를 맞춘다 | **전 모델 재export·재빌드·재측정** |
| `metric3d_v2` 를 16비트로 | 그 모델 재빌드 |
| `depth_anything_v2` 에 uint8 적용 | 그 모델 엔진 교체 |
| 그래프 단순화 통일 | 해당 4개 재export |

**이 결정들은 최적화 레벨 스윕(72회 빌드) 전에 끝내야 한다.**
안 그러면 스윕 결과가 전부 버려진다.

### 4.4 병렬화 — 어디까지 할 것인가

| | 판단 |
| --- | --- |
| 빌드 여러 개 동시에 | **하지 않는다.** 3080 은 10GB 이고 depth_pro·VGGT 빌드가 메모리를 크게 쓴다 |
| 노트북 작업과 데스크탑 작업 병행 | **한다.** A(전처리)와 B(최적화)는 서로 독립 |
| export(CPU 위주)와 빌드(GPU) 겹치기 | 이론상 가능하나 **권장하지 않는다** — 얻는 것에 비해 실패 원인이 늘어난다 |
| 타이밍 캐시 재사용 | **한다.** 스윕에서 빌드 시간을 크게 줄인다. 이미 엔진 옆에 캐시 파일이 생기고 있다 |

### 4.5 필요한 도구 하나

지금 없는 것: **데스크탑에서 긴 작업 여러 개를 순서대로 돌리고, 연결이 끊겨도
살아남고, 어디까지 갔는지 남기는 것.**

최소한으로 필요한 것은 이 정도다. 분산 시스템이 아니라 **스크립트 하나**다.

- 작업 목록 파일 (무엇을 어떤 순서로)
- 순차 실행, 실패해도 다음으로 진행하는 선택지
- 작업별 로그 파일, 그리고 **진행 상태 파일**
- **측정 작업이 도는 동안 빌드 금지** (잠금)

`run.py` 가 이미 "어느 환경에서 무엇을 실행할지"를 알고 있으므로 여기에 얹으면 된다.

### 4.6 실행 순서

```
[노트북]                              [데스크탑]

A1 전처리 일반화 ──────────────┐      B1 metric3d_v2 16비트 시도
   (12개, 바이트 대조)         │      B2 64비트 잔존 검사 12개
        │                      │      A2 그래프 단순화 통일 (4개)
        │                      │              │
        │                      └──────────────┤ ← 엔진을 바꾸는 결정 완료
        │                                     │
        ├─→ C1 테스트 이미지 확대             ├─→ 전부 빌드 → 한 번에 측정
        │                                     │
        └─→ C4 정답 데이터 배선 ──────────────┼─→ C2 16비트 손실 측정
                                              │   C4 정답 대비 정확도 12개
        D 시각화 데모 ────────────────────────┤
                                              └─→ B3 최적화 레벨 스윕 → B4 8비트
```

### 4.7 권장 착수 순서

| | 작업 | 어디서 | 규모 | 선행 |
| --- | --- | --- | --- | --- |
| 1 | **A1 전처리 일반화** | 노트북 | 12개 모델 | — |
| 2 | **B1 `metric3d_v2` 16비트 시도** | 데스크탑 | 하루 안 | — (1과 병행) |
| 3 | **B2 64비트 잔존 검사 12개** | 데스크탑 | 반나절 | — |
| 4 | **A2 그래프 단순화 통일** | 데스크탑 | 4개 export | — |
| 5 | **C1 테스트 이미지 확대** | — | 사진 준비 | 1 |
| 6 | **C4 정답 데이터 1단계 (실내)** | 양쪽 | 1~2일 | 1 |
| 7 | **C2 16비트 손실 측정** | 데스크탑 | 12개 빌드 + 일괄 측정 | 2·3·4 |
| 8 | B3 최적화 레벨 스윕 | 데스크탑 | 72회 빌드, 무인 | 2·3·4 |
| 9 | D 시각화 데모 | 양쪽 | 큼 | 1·5 |

**1번이 병목이다.** 5·6·9 가 전부 여기에 걸려 있고, GPU 가 필요 없으며,
검증이 바이트 단위로 확실하다. **여기서 시작하는 것이 맞다.**

2·3·4 는 1과 독립이므로 **데스크탑에서 동시에 진행할 수 있다.**

---

## 5. 판단이 필요한 것

| | 내용 | 늦추면 생기는 일 |
| --- | --- | --- |
| **인코더 크기** | 같은 급으로 맞출 것인가, 아니면 표에 크기를 병기할 것인가 | 맞추기로 하면 **전 모델 재export·재빌드·재측정.** 스윕 뒤에 정하면 스윕이 버려진다 |
| **uint8 적용** | `depth_anything_v2` 만 해당(−20.7%). 적용하면 그 모델만 엔진 안에서 전처리를 하게 되어 표의 행 의미가 달라진다 | 최종 측정 전에 정해야 한다 |
| **정답 데이터 범위** | 실내만인가, 실외까지인가, 합성까지인가 | 늦어도 무방 — 다른 작업을 막지 않는다 |
| **테스트 이미지** | 어떤 장면들을 넣을 것인가 | 시각화 데모가 막힌다 |
| **3D 뷰어** | 파일로 저장해 별도 프로그램으로 볼 것인가, 브라우저에서 바로 돌려볼 것인가 | 시각화 착수 시점에 필요 |
| **리사이즈 민감도 표** | 기존 6행의 기준선을 코드에서 재구성할 수 없다. 새로 재려면 기준을 정해야 한다 | 늦어도 무방 |

**위 둘(인코더 크기, uint8)은 엔진을 바꾸는 결정이라 먼저 답이 나와야 한다.**
나머지는 해당 작업을 시작할 때 정하면 된다.

---

## 6. 지켜온 원칙 (계속 지킨다)

- **수치는 손으로 적지 않는다.** 측정 → 파일 → 표 자동 생성
- **재현 조건을 함께 적는다.** 입력 크기·정밀도·GPU·드라이버·라이브러리 버전
- **검증 안 된 것을 완료로 표시하지 않는다**
- **하나의 순위로 줄 세우지 않는다.** 입력 크기가 다르면 같은 줄에 놓지 않는다
- **자동으로 조정하지 않는다.** 빌드 설정을 코드가 알아서 바꾸면 측정값을 비교할 수 없게 된다
- **못 한 것은 못 했다고 적는다.** 이유를 모르면 "이유를 모른다"고 적는다
